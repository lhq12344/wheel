# 专利申请文档

## 一种基于影子孪生的预测性安全防护方法及系统

---

# 说明书

## 第一章 技术领域

本发明涉及机器人技术领域，具体涉及一种基于影子孪生的预测性安全防护方法及系统，尤其适用于具有移动底盘和机械臂的移动操作机器人的安全控制。

## 第二章 背景技术

### 2.1 传统机器人安全防护的发展与局限

#### 2.1.1 发展历程

机器人安全防护技术的发展经历了三个主要阶段：

**第一阶段：机械式安全保护（1970年代-1990年代）**

这一阶段的安全防护主要依赖物理手段，包括紧急停止开关、安全插销、机械限位等。典型产品如KUKA早期机器人配备的机械式安全插销，当操作员拔出插销时，机器人立即停止所有运动。这种方式简单可靠，但无法提供任何智能化的防护能力。

**第二阶段：传感式被动保护（1990年代-2010年代）**

随着传感器技术的发展，力矩传感器、触觉传感器开始应用于机器人安全防护。ABB的SafeMove系统通过监测关节力矩来检测碰撞，当力矩异常超过阈值时触发紧急停止。这一阶段的进步在于可以实现碰撞检测后的快速响应，但仍然是"被动响应"式的保护机制。

**第三阶段：视觉感知避障（2010年代至今）**

深度学习技术和视觉传感器的进步推动了基于环境感知的安全防护发展。机器人通过激光雷达、深度相机等传感器构建环境地图，并实时检测障碍物位置进行避障规划。然而，这种方法存在感知盲区，且在复杂动态环境中计算延迟可能导致响应不及时。

#### 2.1.2 传统方法的核心缺陷

传统机器人安全防护方法存在以下根本性缺陷：

**时间维度的滞后性**

所有传统方法都是在碰撞或危险发生之后才能发挥作用。以力矩检测为例，从碰撞发生、力矩传感器感知、控制器处理到电机响应，整个过程通常需要50-100毫秒。在高速运动场景下，100毫秒内机器人可能已经移动了数厘米，足以造成显著的物理损伤。

**空间维度的粗粒度**

基于传感器的防护通常只能检测到特定区域的障碍物。例如，机器人末端安装的力矩传感器只能检测末端的碰撞，无法感知中途路径上的障碍物。基于视觉的防护存在视野盲区，无法检测到被遮挡的障碍物。

**应对能力的局限性**

传统方法难以处理以下复杂场景：

- 高速运动下的动态障碍物轨迹预测
- 机械臂在关节空间中的自碰撞和奇异构型
- 多机器人协作场景中的相互干扰
- 负载变化对安全边界的影响

### 2.2 现有预测性防护方法分析

#### 2.2.1 基于速度限制的安全区域方法

这类方法在机器人周围设定不同级别的安全区域：

- **告警区域**：当检测到障碍物进入时，降低机器人速度
- **安全区域**：进一步降低速度或改变运动方向
- **停止区域**：强制机器人停止运动

**存在的问题**：

1. 安全区域的大小通常是静态设定的，难以适应不同工况
2. 速度的阶梯式变化可能导致运动不平滑
3. 无法精确预测碰撞发生时刻，只能在障碍物接近时被动响应
4. 对于高速运动的小型障碍物，反应时间可能不足

#### 2.2.2 基于规则的安全约束方法

这类方法通过预定义的安全规则限制机器人的运动：

- 最大运动速度限制
- 最小运动距离间隔
- 禁止进入的区域定义
- 关节角度和速度的硬限制

**存在的问题**：

1. 规则通常基于最坏情况设计，导致系统效率低下
2. 难以覆盖所有可能的危险场景
3. 缺乏对动态环境的适应能力
4. 无法处理机械臂奇异性和关节空间碰撞等复杂问题

#### 2.2.3 传统仿真验证方法

传统上，一些高级机器人系统会使用仿真来验证运动规划的安全性：

- 在仿真环境中预测机器人与障碍物的交互
- 检测碰撞后调整轨迹
- 通过仿真结果指导真实机器人的运动

**存在的问题**：

1. 仿真环境与真实环境存在模型误差
2. 计算开销大，难以实现实时验证
3. 无法处理真实环境中的动态变化
4. 仿真中的碰撞检测精度有限

### 2.3 移动操作机器人的特殊挑战

移动操作机器人（Mobile Manipulation Robot）结合了移动底盘和机械臂的功能，在物流、制造业、服务业等领域有广泛应用。然而，这也带来了独特的安全挑战：

**底盘与机械臂的协调问题**

- 底盘运动时机械臂的动态稳定性
- 底盘运动可能带动机械臂末端进入危险区域
- 协调规划中缺乏统一的安全验证机制

**工作空间的复杂性**

- 移动底盘改变了机械臂的基座位置
- 机械臂的可达空间随底盘位置变化
- 障碍物与机器人的相对位置需要实时更新

**运动规划的耦合性**

- 底盘路径规划时需要考虑机械臂的姿态
- 机械臂运动规划时需要考虑底盘的位置约束
- 两者的协同规划增加了计算复杂度

### 2.4 影子孪生概念的创新价值

针对上述问题，本发明提出了一种全新的**影子孪生（Shadow Twin）**概念。影子孪生是指在虚拟空间中创建一个与真实机器人物理隔离的虚拟副本，通过让该虚拟副本超前执行目标命令来验证命令的安全性，从而在危险发生前阻止不安全的命令下发到真实机器人。

这一创新概念具有以下核心价值：

**从被动响应到主动预防的根本转变**

影子孪生通过超前执行机制，在危险实际发生之前就能预测和阻止。这种"预测性防护"的理念从根本上改变了安全防护的范式——从"碰撞后停止"转变为"碰撞前阻止"。

**物理真实性的保障**

与传统的仿真验证不同，影子机器人保持与真实机器人完全相同的物理参数，确保运动行为的一致性。这使得安全验证的结果可以直接信赖，无需担心仿真与现实的差异。

**碰撞隔离的安全保障**

影子机器人与真实机器人之间建立了严格的碰撞隔离机制。这意味着影子中发生的任何碰撞都不会影响真实机器人的正常运行，从而确保了验证过程的绝对安全性。

**统一的安全验证框架**

影子孪生架构可以同时应用于底盘和机械臂，提供统一的安全验证框架。这种架构的通用性使得它可以扩展到各种类型的机器人系统。

## 第三章 发明概述

### 3.1 发明目的

本发明的目的是提供一种基于影子孪生的预测性安全防护方法及系统，以解决现有技术中存在的以下关键问题：

1. **被动响应式安全防护的根本缺陷**：传统方法只能在碰撞发生后响应，无法预防碰撞发生，本发明通过超前执行机制实现主动式防护。

2. **预测能力不足的局限**：现有方法无法精确预测机器人未来运动轨迹与障碍物的相互作用，本发明通过精确的物理仿真实现高置信度的轨迹预测。

3. **机械臂特殊风险的忽视**：现有方法对机械臂奇异性检测和关节空间碰撞检测不完善，本发明通过雅可比矩阵分析实现全面的机械臂安全性验证。

4. **安全策略缺乏适应性的问题**：现有方法的安全边界通常是静态的，难以适应不同负载和工况，本发明通过负载感知的安全边界动态调整机制解决此问题。

5. **协同安全验证的缺失**：现有方法难以同时验证底盘和机械臂的协同安全性，本发明通过统一的影子孪生架构实现协同安全验证。

### 3.2 技术方案概述

本发明采用以下技术方案来实现上述目的：

#### 3.2.1 影子孪生安全防护方法

**一种基于影子孪生的机器人安全防护方法，包括以下步骤：**

S1. **影子机器人创建**：创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享相同的运动学参数、物理参数、控制器参数和关节限位配置。

S2. **状态镜像捕获**：获取真实机器人当前状态的镜像快照，包括位置、速度、关节角度、末端执行器位置等关键信息。

S3. **超前命令执行**：将目标命令同时发送到影子机器人和命令缓冲队列，影子机器人在超前时间窗口（默认为0.35秒）内超前执行该命令。

S4. **实时碰撞检测**：在影子机器人执行命令的过程中，实时检测影子机器人与预设障碍物之间的碰撞状态，包括穿透深度计算和碰撞位置定位。

S5. **多级安全决策**：根据碰撞检测结果生成三级安全决策——允许通行（Allow）、限速通行（Throttle）、阻断通行（Block）。

S6. **决策执行与控制**：若安全决策为允许通行或限速通行，则在超前时间窗口结束后将命令下发至真实机器人；若决策为阻断通行，则阻止命令下发并触发安全警报。

#### 3.2.2 影子孪生安全防护系统

**一种基于影子孪生的机器人安全防护系统，包括：**

- **影子孪生模块**：用于克隆真实机器人并创建物理隔离的影子机器人副本，支持底盘和机械臂的独立克隆与协同工作。

- **碰撞检测模块**：用于检测影子机器人在超前执行命令过程中的碰撞状态，支持精确穿透深度计算和碰撞位置定位。

- **命令缓冲模块**：用于延迟命令执行以实现超前验证，存储目标命令并根据超前时间窗口控制命令的下发时机。

- **安全决策模块**：根据碰撞检测结果和奇异性分析生成Allow/Throttle/Block决策，并根据决策控制命令的下发。

### 3.3 技术效果

与现有技术相比，本发明具有以下显著的技术效果：

#### 3.3.1 预测性防护的实现

通过影子孪生超前执行命令，本发明能够在碰撞发生前预测并阻止危险命令。0.35秒的超前时间窗口提供了充足的安全验证空间，使得系统可以在危险实际发生前做出响应。这种主动式安全防护从根本上改变了安全防护的范式。

#### 3.3.2 精确检测能力

采用Physics.ComputePenetration进行精确碰撞穿透深度计算，检测精度远高于传统的重叠检测方法。穿透深度的精确计算使得系统可以区分轻微接触和严重碰撞，从而做出更准确的安全决策。

#### 3.3.3 多级安全策略

通过Allow/Throttle/Block三级安全决策，本发明在安全性和效率之间取得了精细的平衡。允许状态保证正常工作效率，限速状态在存在风险时降低速度，阻断状态在危险情况下完全阻止命令执行。

#### 3.3.4 奇异性保护机制

基于雅可比矩阵行列式的奇异性检测算法，能够提前识别机械臂的奇异构型。当机械臂接近奇异位置时，系统会采取限速或阻断措施，避免因奇异构型导致的不可控运动。

#### 3.3.5 负载适应性

安全边界根据机器人负载状态动态膨胀。当负载增加时，安全边界相应扩大，提供更大的安全裕度。这种自适应机制确保了在不同工况下的安全性。

#### 3.3.6 广泛的适用性

影子孪生架构可应用于移动底盘、机械臂及移动操作机器人等多种类型的机器人系统。这种通用性使得本发明具有广泛的应用前景。

## 第四章 核心概念与数据模型

### 4.1 影子孪生核心定义

#### 4.1.1 影子机器人（Shadow Robot）

影子机器人是根据真实机器人克隆而成的虚拟机器人副本，其本质特征包括：

**物理真实性**：影子机器人保持与真实机器人完全一致的物理参数，包括质量、惯性矩、摩擦系数、碰撞参数等。这确保了影子机器人的运动行为与真实机器人完全一致，使得安全验证结果可靠。

**层级结构一致性**：影子机器人完整复制真实机器人的Transform层级结构、关节连接关系、运动学模型。每个关节的位置、方向、父子关系都与真实机器人一一对应。

**独立碰撞系统**：影子机器人拥有独立的碰撞检测系统，其碰撞体与真实机器人的碰撞体相互隔离。影子中的碰撞不会触发真实机器人的保护机制，确保验证过程的独立性。

**超前执行特性**：影子机器人在超前时间窗口内执行命令，这个时间窗口通常设置为0.35秒，足以完成完整的安全验证流程。

#### 4.1.2 镜像快照（Mirror Snapshot）

镜像快照是描述真实机器人当前状态的数据结构，是连接真实机器人与影子机器人的关键桥梁。镜像快照包含以下核心数据：

- **底盘世界位置和旋转**：描述底盘在全局坐标系中的位姿
- **底盘平面速度和偏航角速度**：描述底盘当前的运动状态
- **机械臂关节角度和关节速度**：描述机械臂各关节的当前状态
- **末端执行器世界位置**：描述机械臂末端在全局坐标系中的位置

这些数据用于初始化影子机器人的状态，确保影子与真实机器人在命令执行前处于相同的起点。

#### 4.1.3 超前时间（Lead Time）

超前时间是影子机器人超前执行命令的时间间隔，是预测性安全防护的核心参数。默认设置为0.35秒，这一数值的确定考虑了以下因素：

- **通信延迟**：命令从控制程序到执行器的时间
- **计算预算**：安全验证所需的计算时间
- **制动距离**：基于当前速度计算的安全制动距离对应的时间

超前时间的动态调整机制确保在不同工况下都能提供充足的验证时间。

### 4.2 安全决策体系

#### 4.2.1 三级决策类型

**允许通行（Allow）**

触发条件：预测轨迹无碰撞风险，净空充足，无奇异性威胁。

响应动作：命令按原定速度和轨迹执行，不做任何修改。

**限速通行（Throttle）**

触发条件：预测净空不足（但未达到阻断阈值），或接近奇异性阈值。

响应动作：命令按限速比例（throttleRatio）降低速度执行，在安全和效率之间取得平衡。

**阻断通行（Block）**

触发条件：预测碰撞发生，或净空严重不足，或检测到奇异构型。

响应动作：命令被阻止，不下发至真实机器人，同时触发安全警报。

#### 4.2.2 阻断分类体系

阻断决策根据原因分为以下几类：

- **碰撞阻断（GateBlockedByCollision）**：预测轨迹与障碍物发生碰撞
- **死区阻断（GateBlockedByDeadZone）**：目标位于机械臂可达死区内
- **净空不足阻断（GateBlockedByClearance）**：预测净空小于安全阈值
- **奇异性阻断（GateBlockedBySingularity）**：预测到奇异构型

### 4.3 预测状态结构

预测状态用于描述影子机器人超前执行过程中的关键状态信息，包括：

- **预测底盘位姿**：超前时间结束时的预期底盘位置和旋转
- **预测关节角度**：超前时间结束时的预期机械臂关节角度
- **实际超前时间**：完成安全验证的实际超前时间
- **预测最小净空**：整个轨迹上的最小安全净空

## 第五章 影子孪生系统架构

### 5.1 系统整体架构

影子孪生系统采用分层架构设计，各组件协同工作实现预测性安全防护功能：

```
┌─────────────────────────────────────────────────────────────┐
│                    RobotSimulationManager                     │
│                      （系统总管理器）                         │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌───────────────┐  │
│  │  ShadowBaseTwin │  │  ShadowArmTwin  │  │    Shadow     │  │
│  │    Runtime      │  │    Runtime      │  │RobotVisualizer│  │
│  │   （底盘影子）  │  │   （机械臂影子）│  │   （可视化）  │  │
│  └────────┬──────┘  └────────┬──────┘  └───────┬───────┘  │
│           │                    │                   │          │
│           └────────────────────┼───────────────────┘          │
│                                ▼                              │
│              ┌─────────────────────────┐                    │
│              │   ShadowSimulationGate   │                    │
│              │       （安全门控）       │                    │
│              └───────────┬─────────────┘                    │
│                          │                                  │
│           ┌───────────────┼───────────────┐                  │
│           ▼               ▼               ▼                  │
│    ┌────────────┐  ┌──────────────┐  ┌──────────────┐        │
│    │   DiffDrive │  │    Arm6DOF   │  │  Coordinated │        │
│    │   Twin      │  │IKController  │  │ TaskPlanner  │        │
│    └────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

**RobotSimulationManager**：系统总管理器，负责协调各模块工作，维护系统的整体状态。

**ShadowBaseTwinRuntime**：底盘影子孪生运行时，负责底盘影子的创建、同步、碰撞检测和状态管理。

**ShadowArmTwinRuntime**：机械臂影子孪生运行时，负责机械臂影子的创建、同步、碰撞检测和奇异性分析。

**ShadowRobotVisualizer**：影子机器人可视化器，负责在用户界面上显示预测轨迹和影子机器人的运动状态。

**ShadowSimulationGate**：安全门控核心，负责评估命令安全性、生成决策、控制命令下发。

### 5.2 影子底盘孪生运行时详解

#### 5.2.1 功能职责

影子底盘孪生运行时承担以下核心职责：

**物理克隆管理**：创建和管理底盘的物理隔离克隆体，包括层级结构的完整复制、碰撞系统的独立配置、渲染器的特殊设置等。

**参数同步机制**：确保影子底盘与真实底盘的运动参数完全一致，包括运动学参数（最大速度、加速度等）和物理参数（质量、阻力、惯性矩等）。

**延迟命令队列**：实现命令的超前执行机制，队列中的命令根据超前时间窗口延迟执行，确保有足够时间完成安全验证。

**碰撞检测执行**：在影子底盘超前执行命令的过程中，实时检测影子底盘与障碍物的碰撞状态。

#### 5.2.2 物理克隆原理

影子底盘的克隆过程采用Unity的对象实例化机制，但在细节上有多项关键技术：

**完整层级复制**：通过Object.Instantiate方法完整复制真实底盘的GameObject层级结构，包括所有子物体、Transform组件、Collider组件等。这种复制确保了影子底盘在几何结构上与真实底盘完全一致。

**物理参数同步**：克隆完成后，需要将真实底盘的物理参数复制到影子底盘，包括质量（mass）、阻力（drag/angularDrag）、碰撞检测模式（collisionDetectionMode）、运动学模式（isKinematic）等。这些参数决定了影子底盘的运动行为。

**控制器参数同步**：除了物理参数，还需要同步控制器参数，包括速度限制、加速度限制、增益参数、相位转换参数等。这些参数决定了影子底盘的运动规划行为。

**碰撞隔离建立**：影子底盘与真实底盘之间的碰撞检测需要通过Physics.IgnoreCollision设置为相互忽略，确保两个系统完全隔离。

#### 5.2.3 延迟命令队列机制

延迟命令队列是实现超前执行的核心机制，其工作原理如下：

当控制程序发出命令时，该命令同时被发送到两个目标：

1. **命令缓冲队列**：命令被存入缓冲队列，等待超前时间窗口结束
2. **影子底盘**：影子底盘立即开始超前执行该命令

命令缓冲队列根据超前时间窗口（默认0.35秒）延迟命令的下发。只有当影子底盘完成安全验证且未检测到危险时，对应的命令才会被下发至真实底盘。

这种机制确保了真实机器人的命令执行永远滞后于影子机器人的验证执行，从而实现预测性防护。

### 5.3 影子机械臂孪生运行时详解

#### 5.3.1 功能职责

影子机械臂孪生运行时承担以下核心职责：

**关节控制器复制**：克隆真实机械臂的6个关节控制器（OneJointTrapezoidController），每个控制器负责单个关节的梯形轨迹运动。

**ArticulationBody同步**：同步6个关节的ArticulationBody组件参数，包括关节限位、驱动刚度、阻尼、力限等。

**运动验证执行**：在影子机械臂超前执行关节空间轨迹的过程中，检测碰撞风险和奇异性风险。

#### 5.3.2 关节控制器复制原理

影子机械臂的关节控制器复制涉及以下关键技术：

**路径映射机制**：通过计算真实关节到影子关节的相对路径，在影子机械臂中找到对应的关节对象。这种映射确保了每个真实关节都能找到其对应的影子关节。

**控制器组件克隆**：为影子关节添加OneJointTrapezoidController组件，并复制真实控制器的所有配置参数。这确保了影子关节的运动规划行为与真实关节一致。

**驱动参数同步**：同步ArticulationBody的xDrive参数，包括关节限位（lowerLimit/upperLimit）、刚度（stiffness）、阻尼（damping）、力限（forceLimit）等。这些参数决定了关节的驱动特性。

#### 5.3.3 机械臂碰撞检测原理

机械臂碰撞检测比底盘碰撞检测更为复杂，需要考虑以下方面：

**边界盒预检**：首先使用边界盒（Bounds.Intersects）进行快速筛选，排除明显不可能碰撞的情况。这种两阶段检测策略可以大大提高计算效率。

**精确穿透计算**：对于通过边界盒预检的情况，使用Physics.ComputePenetration进行精确的穿透深度计算。该API返回穿透方向和穿透距离，可以精确判断碰撞的严重程度。

**连杆关键点检测**：除了对关节Collider的检测，还需要对机械臂连杆的关键点进行检测。通过运动学计算得到各连杆在世界坐标系中的位置，然后检测这些点是否与障碍物重叠。

**有效穿透阈值**：考虑碰撞体的contactOffset参数，计算有效穿透阈值。只有超过阈值的穿透才被视为真正的碰撞。

### 5.4 影子机器人可视化器

#### 5.4.1 功能概述

影子机器人可视化器负责将影子机器人的状态和预测轨迹以可视化方式呈现给用户，帮助用户理解系统的决策过程。

#### 5.4.2 三种预览模式

**镜像模式（Mirror）**：影子机器人实时跟随真实机器人的状态，用于展示影子系统的当前状态。

**底盘预览模式（BasePreview）**：展示底盘预测运动轨迹，包括目标点和预测路径。

**机械臂预览模式（ArmPreview）**：展示机械臂预测运动轨迹，包括末端执行器的预测路径和各关节的运动状态。

### 5.5 安全门控核心机制

#### 5.5.1 安全门控的地位

安全门控（ShadowSimulationGate）是整个预测性安全防护系统的核心决策单元，位于影子孪生系统与真实机器人控制系统之间。所有发往真实机器人的命令都必须经过安全门控的审查。

#### 5.5.2 底盘命令评估流程

底盘命令评估遵循以下流程：

**轨迹采样**：根据命令的起点和终点，沿轨迹按固定间距（0.08米）分布采样点。

**重叠预检**：对每个采样点，使用OverlapSphere进行快速碰撞筛选。

**精确检测**：对通过重叠预检的采样点，使用ComputePenetration进行精确穿透计算。

**净空计算**：计算每个采样点与最近障碍物之间的距离，得到最小净空值。

**奇异性评估**：对于机械臂命令，还需要计算雅可比矩阵行列式，评估奇异风险。

**决策生成**：根据检测结果，按照阈值判断生成Allow、Throttle或Block决策。

#### 5.5.3 机械臂命令评估的特殊性

机械臂命令评估与底盘命令评估相比具有以下特殊性：

**关节空间轨迹**：机械臂命令是关节角度目标，需要在关节空间中进行轨迹采样和验证。

**连杆碰撞检测**：需要检测机械臂各连杆与障碍物的碰撞，不仅限于末端执行器。

**奇异性检测**：需要计算雅可比矩阵行列式，评估机械臂是否接近奇异构型。

**死区检测**：需要判断目标是否位于机械臂可达空间的死区内。

## 第六章 碰撞检测原理与实现

### 6.1 碰撞检测的重要性

碰撞检测是预测性安全防护的基础。在影子孪生系统中，碰撞检测的准确性直接决定了安全决策的正确性。如果碰撞检测出现漏报，可能导致危险命令被下发至真实机器人；如果出现误报，则会不必要地降低系统效率。

### 6.2 两阶段碰撞检测策略

本发明采用两阶段碰撞检测策略，在检测精度和计算效率之间取得平衡：

**第一阶段：快速重叠检测**

使用边界盒（Bounds）或OverlapSphere进行快速的粗略检测。这一阶段的目的是快速排除大量明显不会碰撞的情况。边界盒检测只涉及简单的几何计算，计算效率很高。

**第二阶段：精确穿透计算**

对于通过第一阶段检测的情况，使用Physics.ComputePenetration进行精确的穿透深度计算。这一阶段会得到精确的穿透方向和穿透距离，可以准确判断碰撞的严重程度。

### 6.3 穿透深度计算的原理

Physics.ComputePenetration是Unity引擎提供的精确碰撞检测API，其原理基于分离轴定理（Separating Axis Theorem，SAT）：

**输入参数**：

- 两个碰撞体及其世界位置和旋转
- 输出穿透方向（从第二个碰撞体指向第一个）
- 输出穿透深度

**计算过程**：

1. 对两个碰撞体的各个面进行法向量测试
2. 找到分离距离最大的面
3. 如果存在分离面，则无碰撞；否则计算穿透深度

**结果解释**：

- 返回true表示存在穿透（即两个碰撞体重叠）
- direction表示从collider2指向collider1的方向
- distance表示穿透的深度

### 6.4 安全边界膨胀机制

为确保安全裕度，本发明采用安全边界膨胀机制：

**基础膨胀量**：为机器人Collider预设基础膨胀量（底盘0.02米，机械臂0.015米）

**负载感知膨胀**：根据负载状态动态调整膨胀系数

**计算公式**：effectiveInflation = baseInflation × (1 + loadFactor × loadInflationGain)

膨胀机制确保了在不确定性和动态变化情况下，系统仍然能够保持足够的安全裕度。

### 6.5 碰撞检测的实时性保障

为确保碰撞检测的实时性，本发明采用以下优化策略：

**障碍物哈希表**：将障碍物集合转换为HashSet，提高查找效率

**空间分区利用**：利用Unity的物理引擎进行空间查询

**计算预算控制**：限制单次检测的计算时间，防止影响主控制循环

## 第七章 奇异性检测原理

### 7.1 机械臂奇异性的概念

机械臂奇异性是机器人运动学中的一个重要概念。当机械臂处于奇异位置时，其雅可比矩阵的秩降低，导致以下问题：

**运动能力丧失**：在某些方向上失去运动能力，无法沿特定方向运动

**速度失控**：关节速度可能趋向无穷大，导致不可控运动

**力控失效**：阻抗控制和力控策略在奇异位置失效

### 7.2 雅可比矩阵与奇异性

对于6-DOF机械臂，其雅可比矩阵是一个6×6的矩阵，将关节空间的速度映射到末端执行器的空间速度：

**线性速度部分**：前3行描述末端线性速度与关节速度的关系

**角速度部分**：后3行描述末端角速度与关节速度的关系

奇异构型发生在雅可比矩阵的行列式为零或接近零时。

### 7.3 Gram行列式检测方法

本发明采用雅可比矩阵的Gram行列式来检测奇异性：

**计算方法**：计算雅可比矩阵线性速度部分与其转置的乘积的行列式

**数学原理**：对于一个m×n的矩阵J，其Gram矩阵G = J·J^T是一个m×m的对称矩阵。G的行列式为零当且仅当J的秩小于m，即存在线性相关性。

**优势**：相对于直接计算6×6矩阵的行列式，3×3矩阵的计算更稳定，数值精度更高。

### 7.4 多级奇异性保护策略

本发明采用多级阈值判断来实现平滑的奇异性保护：

**硬阈值（Hard Threshold）**：当行列式低于此阈值时，生成Block决策，完全阻止命令执行。

**软阈值（Throttle Threshold）**：当行列式低于此阈值但高于硬阈值时，生成Throttle决策，降低执行速度。

**限速比例计算**：根据行列式在两个阈值之间的位置，线性插值计算限速比例。

## 第八章 安全决策机制

### 8.1 三级决策体系

本发明采用三级安全决策体系，在安全性与效率之间取得平衡：

| 决策类型 | 触发条件             | 响应动作                | 典型场景               |
| -------- | -------------------- | ----------------------- | ---------------------- |
| Allow    | 预测无碰撞，净空充足 | 命令按原速原样执行      | 开阔环境，正常操作     |
| Throttle | 净空不足或接近奇异性 | 按throttleRatio限速执行 | 接近障碍物，接近奇异   |
| Block    | 预测碰撞或严重风险   | 阻止命令下发，触发警报  | 碰撞即将发生，严重奇异 |

### 8.2 净空阈值判断

净空（Clearnace）是指机器人与最近障碍物之间的距离。净空阈值判断规则如下：

**阻断阈值（gateBlockClearanceMeters）**：通常设为0.01米。当最小净空小于此值时，表明碰撞迫在眉睫，必须阻断命令。

**限速阈值（gateThrottleClearanceMeters）**：通常设为0.05米。当最小净空小于此值但大于阻断阈值时，表明存在风险，需要限速。

### 8.3 限速比例计算

限速比例（throttleRatio）根据以下因素计算：

**净空-based限速**：根据当前净空在阻断阈值和限速阈值之间的位置，线性插值计算限速比例。

**奇异性-based限速**：根据雅可比行列式在硬阈值和软阈值之间的位置，线性插值计算限速比例。

**最终限速比例**：取两种限速比例的较大值，确保在任一风险存在时都有足够的保护。

### 8.4 决策的时间特性

安全决策具有明确的时间特性：

**决策时刻**：安全决策在命令进入影子系统时立即生成

**有效期**：决策在超前时间窗口内有效

**重评估机制**：如果环境发生重大变化，系统可以重新评估待执行命令的安全性

## 第九章 超前时间机制

### 9.1 超前时间的意义

超前时间（Lead Time）是影子孪生系统的核心时间参数，指影子机器人超前执行命令的时间间隔。默认设置为0.35秒。

超前时间的存在确保了：

1. **安全验证的完成**：有足够时间完成碰撞检测和决策生成
2. **通信延迟的补偿**：抵消命令传输和执行的总延迟
3. **制动距离的覆盖**：覆盖当前速度下的安全制动距离

### 9.2 动态超前时间计算

超前时间并非固定不变，而是根据当前状态动态调整：

**基础窗口**：考虑通信延迟和计算预算的最小时间

**制动距离时间**：基于当前速度计算的制动时间

**最大关节速度时间**：基于机械臂关节速度计算的制动时间

**最终超前时间**：取上述各因素的最大值，确保在最坏情况下也有充足的超前时间。

### 9.3 命令时间线缓冲机制

命令时间线缓冲系统管理所有待执行命令的执行顺序和时间：

**命令入队**：命令进入缓冲区时记录入队时间和序列号

**时间戳计算**：根据超前时间计算命令的就绪时间

**顺序保证**：确保命令按入队顺序执行，避免乱序

**实时更新**：支持在命令执行过程中更新状态信息

## 第十章 实施方式

### 10.1 移动机器人底盘实施例

#### 10.1.1 场景描述

本实施例展示影子孪生技术在差速驱动移动底盘上的应用，典型场景包括：

- 仓库环境中，移动底盘需要安全躲避静态货架和动态人员
- 操作员通过点击界面试图设置目标点，底盘需要精确移动到该位置
- 环境包含已知的静态障碍物和需要实时感知的动态障碍物

#### 10.1.2 工作流程

**S1. 命令下发阶段**

当操作员点击设置目标点时，目标点命令被同时发送到：

- 影子底盘孪生运行时的命令缓冲队列
- 安全门控的评估模块

**S2. 影子超前执行阶段**

影子底盘在0.35秒超前时间窗口内超前执行目标点指令。影子底盘的控制器根据梯形速度曲线规划运动，同时：

- 沿轨迹采样检测碰撞风险
- 计算最小净空
- 评估路径安全性

**S3. 安全决策生成阶段**

安全门控根据影子底盘的反馈生成决策：

- 若轨迹无障碍物且净空充足：生成Allow决策
- 若净空不足：生成Throttle决策并计算限速比例
- 若检测到碰撞：生成Block决策并记录障碍物信息

**S4. 命令执行阶段**

根据决策类型执行不同操作：

- Allow：超前时间结束后，将命令下发至真实底盘
- Throttle：修改命令速度参数后下发
- Block：阻止命令下发，向用户报告障碍物位置

### 10.2 机械臂实施例

#### 10.2.1 场景描述

本实施例展示影子孪生技术在6-DOF机械臂上的应用，典型场景包括：

- 工业装配线上，机械臂需要从托盘中抓取工件
- 工件周围存在夹具和其他障碍物
- 机械臂需要避开奇异构型，保证运动平滑性

#### 10.2.2 工作流程

**S1. IK求解与轨迹规划阶段**

首先，IK求解器计算目标工件的关节角度解，然后轨迹规划器生成从当前姿态到目标姿态的关节空间轨迹。

**S2. 影子关节执行阶段**

影子机械臂在超前时间窗口内执行关节空间轨迹。在执行过程中，系统进行以下检测：

**碰撞检测**：

- 沿关节轨迹采样
- 计算各连杆的世界坐标位置
- 检测连杆与障碍物的重叠

**奇异性检测**：

- 计算各采样点的雅可比矩阵
- 计算Gram行列式
- 评估是否接近奇异阈值

**死区检测**：

- 判断末端是否进入可达死区
- 死区内末端无法沿特定方向运动

**S3. 安全决策与执行阶段**

根据检测结果生成决策：

- 若轨迹无碰撞、无奇异、无死区：Allow
- 若接近奇异阈值：Throttle
- 若检测到碰撞或严重奇异：Block

### 10.3 移动操作机器人协同实施例

#### 10.3.1 场景描述

本实施例展示影子孪生技术在完整移动操作机器人上的协同应用，典型场景包括：

- 物流仓库中，移动操作机器人需要从货架上取货并搬运到指定地点
- 整个过程需要同时保证底盘运动安全和机械臂操作安全
- 环境包含货架、托盘、其他机器人等障碍物

#### 10.3.2 协同规划流程

**第一步：底盘影子预演**

影子底盘预演从当前位置到目标停靠位的移动路径，验证：

- 路径上无碰撞风险
- 到达后有足够的净空空间
- 停靠姿态有利于后续机械臂操作

**第二步：停靠位搜索**

在工作空间内搜索满足以下条件的停靠位：

- 底盘可无碰撞停放
- 机械臂可达目标工件
- 机械臂运动不与障碍物碰撞

**第三步：机械臂影子预演**

影子机械臂预演从当前姿态到目标抓取姿态的运动轨迹，验证：

- 运动过程中无碰撞
- 无奇异构型风险
- 抓取姿态稳定可行

**第四步：协调执行**

按规划轨迹协同执行：

- 底盘移动到停靠位
- 机械臂执行抓取动作
- 底盘搬运到目标位置
- 机械臂执行放置动作

## 权利要求书

### 独立权利要求

**权利要求1**

一种基于影子孪生的机器人安全防护方法，其特征在于，包括以下步骤：

S1. 创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享相同的运动学参数、物理参数、控制器参数和关节限位配置；

S2. 获取真实机器人当前状态的镜像快照，所述镜像快照包括机器人当前位置、速度、关节角度和末端执行器位置信息；

S3. 将目标命令同时发送到所述影子机器人和命令缓冲队列，所述影子机器人在超前时间窗口内超前执行该目标命令；

S4. 在所述影子机器人执行命令的过程中，实时检测影子机器人与预设障碍物之间的碰撞状态；

S5. 根据所述碰撞检测结果生成安全决策，所述安全决策类型包括允许通行、限速通行和阻断通行；

S6. 若所述安全决策为允许通行或限速通行，则在所述超前时间窗口结束后将所述命令下发至真实机器人；若所述安全决策为阻断通行，则阻止所述命令下发并触发安全警报。

**权利要求2**

一种基于影子孪生的机器人安全防护系统，其特征在于，包括：

影子孪生模块，用于克隆真实机器人并创建物理隔离的影子机器人副本，所述影子机器人与真实机器人共享相同的控制器参数和物理参数；

碰撞检测模块，用于检测所述影子机器人在超前执行命令过程中的碰撞状态；

命令缓冲模块，用于延迟命令执行以实现超前验证，所述命令缓冲模块存储目标命令并根据超前时间窗口控制命令的下发时机；

安全决策模块，用于根据所述碰撞检测结果生成安全决策，并根据所述安全决策控制是否将所述命令下发至真实机器人。

### 从属权利要求

**权利要求3**

根据权利要求1所述的方法，其特征在于，步骤S4中所述碰撞检测包括：

沿命令执行轨迹按预设间距分布的多个采样点；

对每个所述采样点进行两阶段碰撞检测，第一阶段使用边界盒预检快速排除不可能碰撞的情况，第二阶段对通过预检的采样点使用Physics.ComputePenetration精确计算穿透深度；

根据所述穿透深度判断是否发生碰撞。

**权利要求4**

根据权利要求1所述的方法，其特征在于，步骤S4中所述碰撞检测还包括：

获取影子机械臂的多个连杆关键点位置；

沿关节空间轨迹对所述连杆关键点进行采样；

检测所述连杆关键点是否与障碍物发生重叠。

**权利要求5**

根据权利要求1所述的方法，其特征在于，还包括奇异性检测步骤：

计算影子机械臂执行命令过程中的雅可比矩阵；

计算所述雅可比矩阵的Gram行列式；

当所述行列式小于预设硬阈值时，判定存在奇异性风险并生成阻断决策。

**权利要求6**

根据权利要求5所述的方法，其特征在于，所述奇异性检测还包括多级阈值判断：

当所述行列式小于软阈值但大于硬阈值时，生成限速决策；

当所述行列式小于硬阈值时，生成阻断决策。

**权利要求7**

根据权利要求1所述的方法，其特征在于，还包括负载感知的安全边界膨胀步骤：

获取机器人的当前负载状态；

根据所述负载状态动态调整安全边界膨胀系数，膨胀系数计算公式为：effectiveInflation = baseInflation × (1 + loadFactor × loadInflationGain)。

**权利要求8**

根据权利要求1所述的方法，其特征在于，所述超前时间窗口根据以下因素动态计算：

通信延迟时间；

计算预算时间；

基于制动距离反推的响应时间；

取上述各因素的最大值作为最终超前时间。

**权利要求9**

根据权利要求2所述的系统，其特征在于，所述影子孪生模块包括：

底盘影子孪生子模块，用于克隆差速驱动底盘并管理底盘命令的超前验证；

机械臂影子孪生子模块，用于克隆多自由度机械臂并管理关节命令的超前验证，包括奇异性检测功能。

**权利要求10**

根据权利要求2所述的系统，其特征在于，所述安全决策模块进一步包括：

轨迹采样单元，用于沿命令执行轨迹生成采样点序列；

重叠检测单元，用于对所述采样点进行快速碰撞筛选；

精确检测单元，用于对通过重叠检测的采样点进行精确穿透计算；

决策生成单元，用于根据检测结果生成Allow、Throttle或Block决策。

**权利要求11**

根据权利要求1所述的方法，其特征在于，所述超前时间窗口默认为0.35秒，且可根据机器人当前运动状态动态调整。

**权利要求12**

根据权利要求1所述的方法，其特征在于，步骤S6中所述触发安全警报包括：

记录阻断命令的详细信息和阻断原因；

向用户界面推送障碍物位置信息；

触发日志记录以供后续分析。

---

# 摘要

本发明公开了一种基于影子孪生的预测性安全防护方法及系统。所述方法包括：创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享相同的运动学参数、物理参数和控制器参数；获取真实机器人当前状态的镜像快照；将目标命令同时发送到影子机器人和命令缓冲队列，影子机器人在超前时间窗口内超前执行该命令；在影子机器人执行命令的过程中，通过两阶段碰撞检测精确检测影子机器人与预设障碍物之间的碰撞状态；根据碰撞检测结果、净空计算和奇异性分析生成允许通行、限速通行或阻断通行三种安全决策；若决策为允许或限速，则在超前时间窗口结束后将命令下发至真实机器人；若决策为阻断，则阻止命令下发并触发安全警报。本发明通过影子孪生超前执行命令，能够在碰撞发生前预测并阻止危险命令，实现主动式安全防护；同时通过雅可比矩阵Gram行列式检测机械臂奇异性，通过负载感知的安全边界膨胀机制适应不同工况。本发明适用于移动底盘、机械臂及移动操作机器人等多种类型的机器人系统。

---

_文档版本: v2.0_
_生成时间: 2026-05-07_
_基于项目: wheel (Unity 2022.3.52f1c1)_
_主要改进: 减少代码量，增加技术原理和背景描述_
