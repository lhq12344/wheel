# 专利申请文档

## 一种基于影子孪生的预测性安全防护方法及系统

---

# 说明书

## 第一章 技术领域

本发明涉及机器人技术领域，具体涉及一种基于影子孪生的预测性安全防护方法及系统，尤其适用于具有移动底盘和机械臂的移动操作机器人的安全控制。

### 1.1 适用范围

本发明的技术方案特别适用于以下类型的机器人系统：

**差速驱动移动底盘**：通过两个独立电机驱动左右轮子，实现转向的移动机器人。典型应用包括仓库AGV、服务机器人、巡检机器人等。

**多自由度机械臂**：通常为4-7自由度的串联机械臂，本发明重点针对6自由度工业机械臂。典型应用包括装配机器人、喷涂机器人、搬运机器人等。

**移动操作机器人**：将移动底盘与机械臂组合形成的复合机器人，底盘负责移动定位，机械臂负责精细操作。典型应用包括物流拣货机器人、柔性装配系统、医疗辅助机器人等。

## 第二章 背景技术

### 2.1 工业机器人安全标准与发展现状

#### 2.1.1 主流安全标准体系

目前国际上公认的机器人安全标准主要包括：

**ISO 10218-1/2:2011**：工业机器人安全要求，规定了机器人在设计和操作中的安全要求，包括机械安全、控制安全葬。

**ISO/TS 15066**：协作机器人安全规范，详细规定了人与机器人协作时的安全要求，包括接触力的限制。

**GB 11291.1-2011**：中国国家标准的工业机器人安全要求，等效采用ISO 10218。

在传统工业机器人安全体系中，安全防护主要依赖以下几种措施：

**速度限制（Speed Limitation）**：当人员进入机器人工作区域时，将机器人速度降低到安全水平（通常为250mm/s以下）。这种方法无法精确判断碰撞风险，只能基于人员位置做粗略判断。

**安全区域监控（Safety Zone Monitoring）**：通过光幕、激光扫描器等传感器定义机器人的工作边界。当检测到人员进入时，触发安全停止或降速。这种方法存在响应滞后，无法处理高速运动的小型障碍物。

**力矩限制（Torque Limitation）**：通过监控关节力矩来判断是否发生碰撞。当力矩异常时触发停止。这种方法只能检测已经发生的碰撞，无法预防。

#### 2.1.2 现有标准的局限性

以ISO/TS 15066为例，其规定的协作安全方法包括：

- **安全受监控停止**：人员进入时机器人停止，人员离开后继续
- **速度与距离监控**：根据人员距离动态调整机器人速度
- **功率与力限制**：限制机器人输出的最大功率和接触力

这些方法的核心局限在于：**都是基于事后响应的保护机制，而非基于预测的预防机制**。

在工业实际中，这意味着：

机器人运动速度必须保守设定，以确保在人员进入后的响应时间内不会造成严重伤害。即使机器人可能正在朝向一个空旷区域运动，也必须降速。

安全区域的边界必须足够大，以覆盖机器人在响应时间内可能运动的距离，这限制了生产效率。

对于高速运动场景（如机器人抓取后快速放置），即使轨迹精确已知，也必须假设最坏情况进行保护。

### 2.2 移动操作机器人的特殊安全挑战

#### 2.2.1 底盘运动对机械臂的影响

移动操作机器人的底盘运动时会带动机械臂整体移动，这种耦合效应带来独特的安全问题：

**末端轨迹不确定性**：当底盘执行转向动作时，机械臂末端会相对于惯性坐标系产生复杂的曲线运动。即使机械臂关节保持不动，末端也会因为底盘旋转而产生位置变化。这种效应在底盘原地旋转时尤为明显，机械臂末端可能划出一个半径数米的圆弧。

**姿态变化导致的可达空间变化**：底盘移动会改变机械臂的基座位置和方向，从而改变机械臂的可达空间。某个位置对于当前底盘姿态是可达的，但对于目标底盘姿态可能不可达。

**动态稳定性问题**：底盘的加速减速会导致机械臂承受惯性力。在急停场景下，机械臂可能在惯性作用下继续运动一段距离。

**协调规划的复杂度**：底盘路径规划需要考虑机械臂当前姿态对碰撞检测的影响；机械臂轨迹规划需要考虑底盘可能的位置变化。这两者相互耦合，使安全验证变得复杂。

#### 2.2.2 关节空间碰撞的特殊性

与固定基座机械臂不同，移动操作机器人的机械臂碰撞检测面临独特挑战：

**自碰撞检测复杂**：机械臂各连杆之间可能发生碰撞，尤其在机械臂折叠收纳状态或执行复杂轨迹时。传统的固定基座机械臂可以通过离线标定确定连杆的相对关系，但移动机器人的基座姿态变化使这种标定变得困难。

**末端与连杆的协调运动**：机械臂末端的运动轨迹是各关节运动的综合结果。末端在某一时刻的安全不代表连杆在此时刻也安全。特别是当机械臂穿越奇异区域时，连杆的运动可能与末端预期不一致。

**工作空间的动态变化**：移动机器人的底盘位置变化使机械臂的工作空间不断变化。障碍物可能在底盘移动过程中突然进入或离开机械臂的可达空间。

### 2.3 预测性安全防护的技术路线分析

#### 2.3.1 轨迹预测方法的现状

目前预测性安全防护的研究方向主要包括：

**基于运动模型的轨迹预测**：通过机器人的运动学模型预测其未来位置。这种方法假设机器人在预测时间窗口内保持当前运动状态或遵循规划的运动轨迹。

优点：计算效率高，可以实现实时预测。

缺点：对于规划轨迹的假设可能与实际执行不一致；无法处理环境变化导致的轨迹调整。

**基于传感器融合的环境预测**：通过激光雷达、视觉等传感器感知环境，预测障碍物的未来位置。

优点：可以处理动态障碍物。

缺点：传感器本身有延迟和误差；感知盲区无法处理；计算复杂度高。

**基于仿真的轨迹验证**：在虚拟环境中模拟机器人的运动，检测是否会与环境中的障碍物发生碰撞。

优点：可以精确检测碰撞。

缺点：仿真与现实存在差异；计算开销大，难以实现实时；无法处理动态环境。

#### 2.3.2 现有预测方法的共同缺陷

综合分析现有技术方案，存在以下共同缺陷：

**验证与执行的时间差**：即使通过仿真验证了轨迹的安全性，从验证完成到命令下发之间仍然存在时间差。在这个时间差内，环境可能发生变化，导致已验证的轨迹变得不安全。

**物理真实性的缺失**：大多数仿真验证方法使用的是简化的机器人模型，与真实机器人存在差异。这导致验证结果无法完全信赖。

**孤立验证的局限性**：底盘和机械臂的安全验证通常是孤立进行的，没有考虑两者之间的耦合效应。

### 2.4 影子孪生概念的提出

#### 2.4.1 概念来源与灵感

影子孪生概念的灵感来源于航空航天领域的"双系统"理念。在航空领域，关键系统通常配备备份系统，当主系统出现故障时，备份系统可以接管控制。但这种方法是被动式的——只在故障发生后才会激活。

影子孪生将这一理念向前推进了一步：**不是在故障发生后被动接管，而是在执行前主动验证**。

具体来说，影子孪生是指：

在虚拟空间中创建一个与真实机器人物理隔离的虚拟副本（影子机器人）。这个影子机器人与真实机器人保持相同的结构和物理参数，能够以相同的方式响应控制命令。

当控制程序发出命令时，该命令会同时发送到真实机器人和影子机器人。影子机器人会"超前"执行该命令，在真实机器人执行之前完成整个动作的模拟。

在这个超前执行的过程中，系统会持续检测影子机器人是否会与环境中的障碍物发生碰撞、是否会进入奇异构型等危险状态。

如果检测到危险，系统会在真实机器人开始执行之前阻止该命令，从而避免事故发生。

#### 2.4.2 与传统仿真验证的本质区别

影子孪生与传统的仿真验证方法有本质区别：

| 对比维度   | 传统仿真验证   | 影子孪生                   |
| ---------- | -------------- | -------------------------- |
| 执行时机   | 离线或提前执行 | 在线实时同步执行           |
| 机器人模型 | 简化的近似模型 | 与真实机器人完全一致       |
| 环境同步   | 可能存在延迟   | 与真实环境实时同步         |
| 响应方式   | 验证后修改轨迹 | 验证前拦截命令             |
| 计算资源   | 独立计算       | 与真实机器人共享物理引擎   |
| 结果信赖度 | 需考虑模型误差 | 直接信赖，无需额外安全裕度 |

#### 2.4.3 物理隔离的实现

影子孪生的一个关键特点是"物理隔离"。这意味着：

**碰撞隔离**：影子机器人与真实机器人使用独立的Collider系统，它们相互之间不会触发碰撞响应。这意味着影子中发生的碰撞不会影响真实机器人的正常运行。

**渲染隔离**：影子机器人的渲染组件可以被独立控制，例如可以设置为半透明显示，以便用户理解影子系统的工作状态。

**物理隔离**：影子机器人使用独立的Rigidbody组件，其物理行为与真实机器人隔离，但参数保持一致。

这种物理隔离的设置使得影子系统可以"独立运行"，不会干扰真实机器人的控制。

## 第三章 发明概述

### 3.1 现有技术存在的问题

经过深入分析现有技术，发现存在以下具体问题：

#### 3.1.1 响应滞后问题

现有安全防护方法的响应时间构成如下：

```
总响应时间 = 传感器检测延迟 + 控制器处理时间 + 命令传输延迟 + 电机响应时间
```

典型数值：

- 传感器检测延迟：15-30ms（力矩传感器）或50-100ms（视觉传感器）
- 控制器处理时间：5-10ms
- 命令传输延迟：2-5ms（CAN总线）或10-20ms（工业以太网）
- 电机响应时间：20-50ms

总计：约40-120ms

在机器人运动速度为1m/s时，120ms内机器人可能移动120mm。这个距离足以造成严重的碰撞伤害。

#### 3.1.2 预测精度不足问题

现有的预测方法存在以下精度问题：

**运动模型简化带来的误差**：大多数预测方法使用简化的运动模型，忽略了一些关键因素：

- 电机响应延迟：实际电机无法瞬间达到目标速度
- 负载变化的影响：机械臂负载变化时运动特性会改变
- 机械柔性的影响：大型机械臂在高速运动时会有一定变形

**环境感知的不确定性**：基于传感器的预测受限于传感器的精度和延迟。激光扫描器的角分辨率通常为0.5-1度，这意味着10米外的障碍物位置可能有0.1-0.2米的误差。

**简化碰撞检测带来的漏报**：为了满足实时性要求，许多系统使用简化的碰撞检测算法。例如用球体近似机械臂，用圆柱体近似障碍物。这种简化可能导致某些实际上会发生碰撞的情况被漏报。

#### 3.1.3 奇异性检测缺失问题

机械臂奇异性是机器人运动学中的一个重要概念，但现有安全系统往往忽视这一问题。

**奇异位置的特殊性质**：当机械臂处于奇异位置时，其雅可比矩阵的秩降低，导致以下问题：

- 末端在某些方向上失去运动能力
- 为了实现末端的微小运动，关节需要做出巨大变化
- 关节速度可能趋向无穷大
- 力和位置控制策略失效

**现有标准的处理**：现有的机器人安全标准（如ISO 10218）对奇异性问题没有明确要求。大部分工业机器人通过软件限制来避免进入奇异区域，但这种限制往往是粗糙的全局限制，无法针对具体任务进行优化。

**预测奇异性的困难**：准确预测机械臂是否会进入奇异构型需要计算运动过程中的雅可比矩阵变化。这对于实时系统来说计算开销较大。

### 3.2 技术方案

#### 3.2.1 整体架构

本发明提出一种基于影子孪生的预测性安全防护系统，其架构如下：

```
┌─────────────────────────────────────────────────────────────┐
│                      RobotSimulationManager                  │
│                       (系统总管理器)                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                    影子孪生层                          │ │
│  │  ┌─────────────────┐    ┌─────────────────┐            │ │
│  │  │  ShadowBaseTwin │    │  ShadowArmTwin  │            │ │
│  │  │    Runtime      │    │    Runtime      │            │ │
│  │  │   底盘影子       │    │   机械臂影子    │            │ │
│  │  └────────┬────────┘    └────────┬────────┘            │ │
│  │           │                      │                       │ │
│  │           └──────────┬───────────┘                       │ │
│  │                      ▼                                   │ │
│  │             ┌───────────────┐                          │ │
│  │             │   Shadow     │                           │ │
│  │             │SimulationGate│                           │ │
│  │             │   安全门控   │                           │ │
│  │             └───────┬───────┘                          │ │
│  └─────────────────────┼───────────────────────────────────┘ │
│                        │                                      │
│  ┌─────────────────────┼───────────────────────────────────┐ │
│  │                真实控制层                                 │ │
│  │                      ▼                                   │ │
│  │  ┌────────────┐ ┌──────────────┐ ┌──────────────┐      │ │
│  │  │  DiffDrive │ │    Arm6DOF   │ │  Coordinated │      │ │
│  │  │  Twin      │ │IKController  │ │ TaskPlanner  │      │ │
│  │  │ Controller │ │              │ │              │      │ │
│  │  └────────────┘ └──────────────┘ └──────────────┘      │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.2 核心组件功能

**影子孪生模块（Shadow Twin Module）**

影子孪生模块负责创建和管理影子机器人。具体包括：

ShadowBaseTwinRuntime：底盘影子孪生运行时

- 克隆真实底盘的层级结构
- 同步控制器参数和物理参数
- 管理延迟命令队列
- 执行超前轨迹验证

ShadowArmTwinRuntime：机械臂影子孪生运行时

- 克隆真实机械臂的6个关节
- 复制关节控制器和驱动参数
- 执行关节空间轨迹验证
- 检测碰撞和奇异性

**安全门控模块（ShadowSimulationGate）**

安全门控是系统的核心决策单元：

- 接收来自控制层的命令
- 在影子孪生中验证命令安全性
- 生成Allow/Throttle/Block决策
- 控制命令是否下发至真实机器人

**命令缓冲模块（Command Buffer）**

命令缓冲模块实现命令的延迟下发：

- 存储待执行的命令
- 根据超前时间窗口控制下发时机
- 保证命令执行顺序
- 支持命令的修改和撤回

#### 3.2.3 命令执行流程

当控制程序发出命令时，系统按以下流程处理：

**阶段1：命令接收**

控制程序通过SetTargetPoint、SetVelocityCommand等接口发出运动命令。这些命令同时被发送到两个目标：

- 命令缓冲队列：等待验证完成后下发
- 影子孪生模块：开始超前验证

**阶段2：影子验证**

影子孪生模块接收到命令后：

1. 同步真实机器人状态到影子机器人（Mirror Snapshot）
2. 将命令应用到影子机器人
3. 影子机器人在超前时间窗口内（默认0.35秒）执行命令
4. 在执行过程中，安全门控持续检测：
   - 影子机器人是否与障碍物发生碰撞
   - 影子机械臂是否接近奇异构型
   - 预测轨迹是否满足净空要求

**阶段3：决策生成**

安全门控根据验证结果生成决策：

- **Allow（允许）**：验证通过，命令安全
- **Throttle（限速）**：存在风险，按限速比例执行
- **Block（阻断）**：存在危险，阻止命令执行

**阶段4：命令下发**

根据决策类型：

- Allow：命令原样下发至真实机器人
- Throttle：命令经过限速处理后下发
- Block：命令被丢弃，触发安全警报

### 3.3 关键参数设置

#### 3.3.1 超前时间设置

超前时间（Lead Time）是影子孪生系统的核心参数，其设置需要考虑以下因素：

**通信延迟（Communication Latency）**

命令从控制程序到执行器的时间，包括：

- 控制程序内部的调度延迟：约2-5ms
- 总线传输延迟（CAN/Ethernet）：约5-15ms
- 驱动器响应延迟：约2-5ms

总计约10-25ms。

**计算预算（Compute Budget）**

安全验证所需的计算时间，包括：

- 碰撞检测计算：约5-10ms
- 奇异性检测计算：约3-5ms
- 决策生成：约1-2ms

总计约10-17ms。

**制动距离（Braking Distance）**

基于当前速度计算的安全制动时间：

- 底盘制动：速度 / 减速度
- 例如：1m/s速度，0.9m/s²减速度，制动时间约1.1s

**最终超前时间**

取上述各因素的最大值，并考虑一定余量。本发明默认设置为0.35秒，这是一个在安全性和实时性之间取得平衡的值。

#### 3.3.2 安全阈值设置

**阻断净空阈值（gateBlockClearanceMeters）**

当预测净空小于此值时，命令被阻断。默认设置为0.01米（10毫米）。

这个数值的确定考虑了以下因素：

- 碰撞检测的数值精度
- 机器人控制系统的位置误差
- 一定的安全余量

**限速净空阈值（gateThrottleClearanceMeters）**

当预测净空小于此值但大于阻断阈值时，命令限速执行。默认设置为0.05米（50毫米）。

这个数值确保在接近障碍物时有足够的减速距离。

**奇异性硬阈值（singularityHardThreshold）**

当雅可比矩阵Gram行列式小于此值时，命令被阻断。默认设置为1.0×10⁻⁴。

**奇异性软阈值（singularityThrottleThreshold）**

当雅可比矩阵Gram行列式小于此值但大于硬阈值时，命令限速执行。默认设置为2.5×10⁻⁴。

### 3.4 技术效果

本发明相比现有技术取得以下技术效果：

#### 3.4.1 预测时间的确定性保障

通过0.35秒的超前时间窗口，系统有确定的时间完成安全验证。这个时间窗口足够覆盖：

- 通信延迟：25ms
- 计算预算：17ms
- 制动距离对应时间：可达1s以上

即使在最坏情况下（高速运动），系统也有充足时间完成验证并做出响应。

#### 3.4.2 物理真实性的保证

影子机器人与真实机器人共享相同的物理参数，包括：

- 质量分布
- 惯性矩
- 摩擦系数
- 驱动刚度和阻尼

这确保了影子机器人的运动行为与真实机器人完全一致，使得安全验证结果可以直接信赖。

#### 3.4.3 多维度安全检测

本发明同时检测以下安全维度：

- **碰撞检测**：检测影子机器人是否与障碍物发生碰撞
- **净空检测**：检测预测轨迹与障碍物的最小距离
- **奇异性检测**：检测机械臂是否接近奇异构型
- **死区检测**：检测目标是否位于机械臂可达死区

#### 3.4.4 灵活性与安全性的平衡

通过三级安全决策，系统可以在不同风险等级下采取适当的响应：

- 无风险时保持全速运行，保证效率
- 有风险时降速运行，在安全和效率间取得平衡
- 有危险时完全阻止，保护人员和设备安全

## 第四章 系统架构详细说明

### 4.1 影子底盘孪生运行时的结构

#### 4.1.1 物理克隆的实现

影子底盘的克隆过程遵循以下步骤：

**步骤1：获取源对象**

通过GetComponentInChildren方法获取真实底盘的根Transform。这个根对象包含了底盘的完整层级结构。

**步骤2：实例化克隆对象**

使用Unity的Object.Instantiate方法克隆整个层级。这个方法会递归克隆所有子物体，包括Transform、Rigidbody、Collider等组件。

克隆后的对象名称设置为"Preview*ShadowBaseTwin*{原始名称}"，以便识别。

**步骤3：设置隐藏标志**

克隆对象设置HideFlags.DontSave标志，确保：

- 不会被场景保存
- 不会出现在层级视图中
- 不会参与游戏构建

**步骤4：挂载到管理器**

克隆对象挂载到RobotSimulationManager下方，以便统一管理。

#### 4.1.2 参数同步的细节

影子底盘需要同步的参数包括以下类别：

**运动学参数**

- 最大线速度（vMax）：真实底盘可以执行的最大直线速度
- 最大线加速度（aMax）：真实底盘可以执行的最大直线加速度
- 最大角速度（wMax）：真实底盘可以执行的最大旋转速度
- 最大角加速度（alphaMax）：真实底盘可以执行的最大角加速度

**控制参数**

- 距离增益（kDist）：控制器的距离误差增益
- 航向增益（kYaw）：控制器的航向误差增益
- 原地旋转角度阈值（rotateInPlaceAngleDeg）：超过此角度差时执行原地旋转
- 位置容差（posTolerance）：认为到达目标的容差距离
- 到达抓取距离（arrivalSnapDistance）：到达此距离时抓取目标位置

**物理参数**

通过Rigidbody组件同步：

- 质量（mass）
- 阻力（drag、angularDrag）
- 重力设置（useGravity）
- 运动学模式（isKinematic）
- 插值模式（interpolation）
- 碰撞检测模式（collisionDetectionMode）

#### 4.1.3 碰撞隔离的实现

影子底盘与真实底盘之间的碰撞隔离通过以下代码实现：

遍历真实机器人上的所有Collider（\_liveRobotColliders）与影子机器人上的所有Collider（\_shadowBaseColliders），对每一对Collider调用Physics.IgnoreCollision设置为相互忽略。

这个设置确保：

- 真实机器人的Collider不会与影子机器人的Collider触发碰撞响应
- 影子中的碰撞不会触发真实机器人的传感器或保护机制

#### 4.1.4 延迟命令队列的实现

延迟命令队列的数据结构如下：

每个延迟命令包含：

- 命令类型（type）：Velocity、TargetPoint、TargetYaw
- 执行时间戳（executeRealtime）：命令应该被执行的时间
- 命令参数：线速度、角速度、目标点等

当命令入队时，执行时间戳被设置为当前时间加上超前时间（\_leadSeconds）。

在每个Tick中，系统检查队列头部的命令是否到了执行时间。如果到了执行时间且安全验证通过，命令才会被下发到真实底盘。

### 4.2 影子机械臂孪生运行时的结构

#### 4.2.1 关节映射机制

影子机械臂的关节与真实机械臂的对应关系通过路径映射建立：

**步骤1：计算相对路径**

对于真实机械臂上的每个关节，计算它相对于机械臂根目录的路径。例如：`Joint01/Joint02/Joint03`

**步骤2：查找影子关节**

在影子机械臂中使用Find方法查找相同路径的Transform。如果找到，则认为这是对应关节。

**步骤3：获取组件**

获取该Transform上的ArticulationBody组件，这就是影子关节。

这种映射机制确保即使Unity在实例化时改变了对象的实例ID，只要层级结构保持一致，关节的对应关系就不会出错。

#### 4.2.2 控制器复制的细节

每个关节的梯形轨迹控制器（OneJointTrapezoidController）需要完整复制以下参数：

- 目标角度（goalDeg）
- 最大速度（maxSpeedDegPerSec）
- 最大加速度（maxAccelDegPerSec2）
- 起跳速度（kickSpeedDegPerSec）
- 容差角度（toleranceDeg）

同时需要配置ArticulationBody的xDrive参数：

- 关节限位（lowerLimit、upperLimit）
- 驱动刚度（stiffness）
- 驱动阻尼（damping）
- 力限（forceLimit）

#### 4.2.3 可视化配置

影子机械臂的渲染配置如下：

- 半透明青蓝色材质（Color(0.1f, 0.85f, 1f, 0.38f)）
- 透明渲染队列（renderQueue = Transparent）
- Alpha混合模式（SrcAlpha / OneMinusSrcAlpha）
- 关闭深度写入（ZWrite = 0）

这种配置使得影子机械臂可以在不影响真实机器人渲染的情况下显示，并且用户可以直观地看到影子机械臂的运动状态。

### 4.3 安全门控的决策逻辑

#### 4.3.1 底盘命令评估流程

当安全门控接收到底盘命令时，执行以下评估：

**步骤1：解析命令**

从BaseGateCommand中提取起点位置、终点位置、命令类型等信息。

**步骤2：计算采样点**

根据起点和终点之间的距离，计算需要的采样点数量：

- 基于距离的采样：距离 / 采样间距（0.08米）
- 基于时间的采样：超前时间 / 固定时间步长
- 最终采样数取两者较大值，最多不超过64个

**步骤3：沿轨迹检测**

对于每个采样点：

1. 检查该点是否与障碍物重叠（OverlapSphere）
2. 如果重叠，使用ComputePenetration计算精确穿透深度
3. 计算该点与最近障碍物的距离（净空）

**步骤4：决策生成**

根据检测结果：

- 如果任何采样点穿透深度超过阈值：Block（碰撞阻断）
- 如果最小净空小于阻断阈值：Block（净空不足阻断）
- 如果最小净空小于限速阈值：Throttle（限速通行）
- 否则：Allow（允许通行）

#### 4.3.2 机械臂命令评估流程

当安全门控接收到机械臂命令时，执行以下评估：

**步骤1：解析命令**

从ArmGateCommand中提取起始关节角度、目标关节角度等信息。

**步骤2：碰撞守卫评估**

首先调用Arm6DOFIKController.EvaluateMotionCollision进行快速碰撞检测。这个方法会检测关节轨迹上的连杆是否会与障碍物发生碰撞。

**步骤3：沿轨迹采样**

在关节空间中对轨迹进行采样：

1. 计算采样数量：角度差 / 角度采样间距（4度）
2. 对每个采样点计算雅可比矩阵
3. 计算Gram行列式进行奇异性检测
4. 使用ComputeSphereClearance计算连杆关键点与障碍物的距离

**步骤4：决策生成**

根据检测结果：

- 如果碰撞守卫检测到碰撞：Block
- 如果任何采样点雅可比行列式小于硬阈值：Block（奇异性阻断）
- 如果任何采样点Gram行列式小于软阈值：Throttle
- 如果任何连杆关键点净空小于阈值：Block
- 否则：Allow

#### 4.3.3 限速比例的计算

限速比例的计算遵循以下公式：

对于净空-based限速：

```
normalized = (minClearance - blockThreshold) / (throttleThreshold - blockThreshold)
throttleRatio = lerp(0.25, 0.85, normalized)
```

对于奇异性-based限速：

```
normalized = (determinant - hardThreshold) / (throttleThreshold - hardThreshold)
throttleRatio = lerp(0.2, 0.85, normalized)
```

最终限速比例取两种计算结果的较大值。

### 4.4 负载感知的安全边界膨胀

#### 4.4.1 负载系数的获取

负载系数（loadFactor）的获取方式包括：

- 直接测量：通过力传感器测量末端负载
- 估计值：根据当前任务估算（例如抓取物体时）
- 固定值：对于未知负载使用保守估计

#### 4.4.2 膨胀系数的计算

膨胀系数的计算公式：

```
effectiveInflation = baseInflation × (1 + loadFactor × loadInflationGain)
```

其中：

- baseInflation：基础膨胀量（底盘0.02m，机械臂0.015m）
- loadFactor：当前负载系数
- loadInflationGain：负载膨胀增益（默认0.35）

**示例计算**：

假设负载系数为0.8（较高负载），基础膨胀量为0.02m：

```
effectiveInflation = 0.02 × (1 + 0.8 × 0.35) = 0.02 × 1.28 = 0.0256m
```

即安全边界膨胀了28%。

这种机制确保在高负载情况下，机器人有更大的安全裕度，因为高负载会影响制动距离和运动精度。

## 第五章 碰撞检测原理

### 5.1 两阶段碰撞检测策略

#### 5.1.1 第一阶段：快速预检

使用边界盒（Bounds）或OverlapSphere进行快速筛选：

**Bounds.Intersects方法**：

对于两个碰撞体，首先获取它们的边界盒（包围盒）。边界盒是一个长方体，完全包围碰撞体的形状。

如果两个边界盒不相交，则两个碰撞体必然不相交，可以快速排除。

这种方法计算简单，适合作为第一层筛选。

**OverlapSphere方法**：

在采样点位置创建一个球体，检测该球体与障碍物的重叠情况。

球体的半径等于机器人等效半径加上安全膨胀量。

如果球体不与任何障碍物重叠，则该采样点安全。

#### 5.1.2 第二阶段：精确检测

对于通过预检的情况，使用Physics.ComputePenetration进行精确检测：

**方法签名**：

```csharp
bool ComputePenetration(
    Collider collider1,
    Vector3 position1,
    Quaternion rotation1,
    Collider collider2,
    Vector3 position2,
    Quaternion rotation2,
    out Vector3 direction,
    out float distance
);
```

**参数说明**：

- collider1, position1, rotation1：第一个碰撞体及其世界位置和旋转
- collider2, position2, rotation2：第二个碰撞体及其世界位置和旋转
- direction：输出的穿透方向（从collider2指向collider1）
- distance：输出的穿透深度

**返回值**：如果两个碰撞体存在穿透，返回true；否则返回false。

**结果解读**：

当返回true时：

- direction表示将collider2移开以消除穿透的方向
- distance表示需要移动的距离

例如，如果distance=0.05，表示两个碰撞体相互嵌入5厘米。

#### 5.1.3 阈值的确定

有效穿透阈值（GetEffectivePenetrationThreshold）的计算考虑了两个碰撞体的contactOffset：

```csharp
threshold = max(0.0001,
    safeContactOffset(collider1) + safeContactOffset(collider2))
```

contactOffset是Unity碰撞系统的参数，用于在碰撞检测中预留一定的间隙。

如果穿透深度小于阈值，说明两个碰撞体只是接触而非真正嵌入，这可能是正常的物理接触，不需要触发碰撞响应。

### 5.2 连杆关键点检测

#### 5.2.1 关键点的定义

对于6-DOF机械臂，需要检测的关键点包括：

- 关节位置（6个）：每个关节的当前位置
- 连杆中点（5个）：相邻关节之间线段的中点
- 末端执行器（1个）：机械臂末端的当前位置

共12个关键点需要在每个采样点进行检测。

#### 5.2.2 检测方法

关键点检测通过以下步骤实现：

**步骤1：获取关节角度**

从MirrorSnapshot或直接通过控制器获取当前6个关节的角度值。

**步骤2：计算正向运动学**

使用Arm6DOFFKController.ComputeLinkPosesWorld方法计算各连杆的世界坐标位置。这个方法基于DH参数或直接变换矩阵计算。

**步骤3：检测每个关键点**

对于每个关键点，使用OverlapSphere检测是否与障碍物重叠。

### 5.3 机械臂碰撞守卫

#### 5.3.1 碰撞守卫的原理

碰撞守卫（ArmCollisionGuard）是一种快速检测关节空间轨迹碰撞的方法。

它的基本思想是：将轨迹离散化为多个采样点，对每个采样点检测机械臂是否会与障碍物发生碰撞。

**优点**：可以精确检测任意轨迹的碰撞

**缺点**：计算量大，需要对轨迹进行密集采样

#### 5.3.2 实现细节

碰撞守卫的实现在Arm6DOFIKController.EvaluateMotionCollision方法中：

1. 将轨迹从起始关节角度插值到目标关节角度
2. 在关节空间中等间距采样（默认4度间隔）
3. 对每个采样点计算机械臂的连杆位置
4. 检测每个连杆是否与障碍物发生碰撞

如果检测到碰撞，返回碰撞信息：

- 碰撞发生的关节角度
- 碰撞涉及的连杆
- 碰撞位置

## 第六章 奇异性检测原理

### 6.1 雅可比矩阵基础

#### 6.1.1 机械臂雅可比矩阵的定义

对于n-DOF机械臂，雅可比矩阵J是一个6×n的矩阵，将关节空间的速度映射到末端执行器的空间速度：

```
v = J × q̇
```

其中：

- v：末端执行器的空间速度（6维：3维线速度 + 3维角速度）
- q̇：关节速度（n维）
- J：雅可比矩阵（6×n）

雅可比矩阵的每一列对应一个关节对末端速度的影响。

#### 6.1.2 雅可比矩阵的计算

雅可比矩阵可以通过分析法和数值法计算。

分析法根据机械臂的几何结构直接推导，计算效率高但需要针对特定机械臂结构。

数值法通过扰动关节并观察末端变化来近似计算，通用性强但计算量大。

本发明采用分析法计算雅可比矩阵，针对6-DOF机械臂的结构特点进行优化。

### 6.2 Gram行列式与奇异性

#### 6.2.1 奇异性的数学定义

当机械臂处于奇异构型时，雅可比矩阵的秩降低，其行列式为零或接近零。

对于6-DOF机械臂，其雅可比矩阵是6×6的方阵。如果矩阵的行列式为零，说明矩阵不满秩，存在线性相关性。

在实际机械臂中，奇异构型通常发生在：

- 关节1和关节4对齐时（肩部奇异）
- 关节4、5、6共线时（肘部奇异）
- 末端到达工作空间边界时（腕部奇异）

#### 6.2.2 Gram行列式的计算

直接计算6×6矩阵的行列式可能面临数值稳定性问题。

本发明采用Gram行列式方法来检测奇异性：

1. 提取雅可比矩阵的角速度部分（后3行，6列），这是一个3×6的矩阵
2. 计算J·J^T，得到一个3×3的矩阵
3. 计算这个3×3矩阵的行列式

数学原理：对于m×n的矩阵J，矩阵J·J^T的行列式为零当且仅当rank(J) < m。

由于我们只关心机械臂的角速度部分（线速度部分在大部分应用中影响较小），使用3×3矩阵的行列式可以更稳定地检测奇异性。

#### 6.2.3 计算公式

设J_angular为雅可比矩阵的后3行（角速度部分），则：

```
G = J_angular × J_angular^T  (3×3矩阵)

det(G) = | G |
```

最终得到的是一个标量值，表示关节空间速度与末端角速度之间的耦合程度。

当det(G)趋近于零时，说明存在某个方向，末端角速度无法通过关节速度实现，即发生奇异。

### 6.3 阈值确定与多级保护

#### 6.3.1 硬阈值的确定

硬阈值（block threshold）设置为1.0×10⁻⁴，这个数值的确定考虑了：

**数值稳定性**：小于此值时，数值计算的误差可能变得显著

**实际奇异距离**：在此值以下，机械臂的运动特性已经开始显著偏离预期

**响应时间**：确保在进入硬阈值之前有足够的预警时间

#### 6.3.2 软阈值的确定

软阈值（throttle threshold）设置为2.5×10⁻⁴，这个数值确保：

- 比硬阈值有更大的预警余量
- 允许系统在接近奇异时采取限速措施
- 为操作员或规划器提供反应时间

#### 6.3.3 多级保护的效果

通过设置两级阈值，实现了平滑的保护机制：

- 在软阈值和硬阈值之间：限速执行，给系统适应时间
- 在硬阈值以下：完全阻止，避免失控风险

这种机制避免了系统在奇异点附近出现不稳定的开关行为。

## 第七章 实施例

### 7.1 仓储物流机器人实施例

#### 7.1.1 应用场景描述

某电商仓库使用移动操作机器人进行拣货作业。机器人的配置如下：

**底盘参数**：

- 类型：差速驱动
- 最大速度：1.5m/s
- 最大加速度：0.9m/s²
- 等效半径：0.35m

**机械臂参数**：

- 类型：6-DOF串联机械臂
- 最大工作半径：0.85m
- 各关节范围：Joint1 ±180°，Joint2 -90°~90°，Joint3 -90°~90°，Joint4 ±180°，Joint5 ±120°，Joint6 ±360°

**工作环境**：

- 货架高度：2.2m
- 通道宽度：1.2m
- 货物箱尺寸：0.4m × 0.3m × 0.2m
- 障碍物：货架立柱、其他机器人、操作人员

#### 7.1.2 拣货作业流程

拣货作业的标准流程如下：

**步骤1：导航到目标货架**

操作人员通过手持终端下发目标货架位置。机器人规划路径并导航到目标位置。

在这个过程中，影子底盘会超前验证移动路径的安全性：

1. 沿路径采样64个点
2. 检测每个采样点是否与货架立柱或其他障碍物重叠
3. 计算每个采样点的净空
4. 根据最小净空生成决策

**步骤2：调整姿态**

到达货架后，机器人需要调整底盘姿态，使机械臂能够到达目标货物位置。

影子系统会验证目标姿态是否满足以下条件：

- 机械臂可达目标货物
- 机械臂运动过程无碰撞
- 不会进入奇异构型

**步骤3：抓取货物**

IK求解器计算抓取姿态的关节角度。影子机械臂验证该轨迹：

1. 碰撞守卫检测
2. 雅可比行列式检测
3. 连杆关键点检测

如果验证通过，命令下发到真实机械臂执行抓取。

**步骤4：转运到暂存区**

抓取成功后，机器人移动到暂存区放下货物。这个过程同样需要通过影子系统验证安全性。

#### 7.1.3 异常处理

**情况1：路径上出现障碍物**

如果在影子验证过程中检测到路径上有障碍物（例如突然出现的其他机器人），系统会：

- 生成Block决策
- 阻止命令下发
- 向操作人员报警，提示障碍物位置

**情况2：接近奇异区域**

如果检测到轨迹会接近奇异区域（例如关节2接近-90°），系统会：

- 生成Throttle决策
- 限制机械臂运动速度
- 允许操作继续但降低效率

**情况3：负载变化**

当机械臂抓取不同重量的货物时，安全边界会动态调整：

- 空载时，安全边界最小
- 满载时，安全边界膨胀约28%

这确保了在不同负载下都有足够的安全裕度。

### 7.2 工业装配机器人实施例

#### 7.2.1 应用场景描述

某汽车零部件装配线使用固定基座的6-DOF机械臂进行螺栓拧紧作业。场景特点：

- 机械臂固定安装，基座不移动
- 工作空间内有多个工装夹具和其他机械臂
- 要求高节拍、高精度

#### 7.2.2 安全验证过程

**点对点运动验证**：

1. 控制程序发出目标关节角度
2. 影子机械臂执行该轨迹
3. 碰撞守卫检测连杆是否会与夹具碰撞
4. 雅可比检测是否会进入奇异区域

**连续轨迹验证**：

对于需要连续运动的场景（如涂胶轨迹）：

1. 将轨迹离散化为多个目标点
2. 对每个目标点进行验证
3. 确保整个轨迹的安全性

#### 7.2.3 限速策略

在接近工装夹具时，系统会：

1. 计算到最近障碍物的距离
2. 根据距离计算限速比例
3. 动态调整运动速度

例如：

- 距离 > 50mm：全速运行
- 距离 20-50mm：限速至75%
- 距离 10-20mm：限速至50%
- 距离 < 10mm：完全停止

### 7.3 人机协作机器人实施例

#### 7.3.1 应用场景描述

协作机器人与人操作员在同一空间工作，需要满足ISO/TS 15066的协作安全要求。

本发明通过影子孪生实现比标准要求更高级别的安全防护。

#### 7.3.2 动态安全区域

传统方法使用静态的安全区域边界。本发明通过影子孪生实现动态的安全区域：

1. 实时获取操作员的位置（通过视觉或激光扫描）
2. 在影子系统中模拟操作员可能进入的区域
3. 根据操作员位置调整机器人的速度限制

#### 7.3.3 预测性防护

当操作员向机器人移动时：

1. 影子系统预测操作员未来的位置
2. 如果预测显示操作员将进入机器人的工作区域，机器人提前降速
3. 这种预测性防护比传统的事后响应安全性更高

## 权利要求书

### 独立权利要求

**1.** 一种基于影子孪生的机器人安全防护方法，其特征在于，包括以下步骤：

S1. 创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享运动学参数、物理参数、控制器参数和关节限位配置，其中物理隔离通过碰撞隔离机制实现，影子机器人的碰撞体与真实机器人的碰撞体相互不触发碰撞响应；

S2. 获取真实机器人当前状态的镜像快照，镜像快照包括底盘世界位置和旋转、底盘平面速度和偏航角速度、机械臂关节角度和关节速度、末端执行器世界位置；

S3. 将目标命令同时发送到影子机器人和命令缓冲队列，影子机器人在超前时间窗口内超前执行该目标命令，超前时间窗口根据通信延迟、计算预算和制动距离动态确定，默认值为0.35秒；

S4. 在影子机器人超前执行命令的过程中，通过两阶段碰撞检测实时检测影子机器人与预设障碍物之间的碰撞状态，第一阶段使用边界盒预检快速排除不可能碰撞的情况，第二阶段对通过预检的情况使用Physics.ComputePenetration精确计算穿透深度；

S5. 根据碰撞检测结果生成安全决策，安全决策类型包括允许通行、限速通行和阻断通行，其中阻断通行在预测到碰撞发生、净空小于安全阈值或检测到奇异构型时触发；

S6. 若安全决策为允许通行或限速通行，则在超前时间窗口结束后将命令下发至真实机器人；若安全决策为阻断通行，则阻止命令下发并触发安全警报。

**2.** 一种基于影子孪生的机器人安全防护系统，其特征在于，包括：

影子孪生模块，用于克隆真实机器人并创建物理隔离的影子机器人副本，影子机器人与真实机器人共享相同的控制器参数和物理参数，物理隔离通过碰撞隔离机制实现；

碰撞检测模块，用于检测影子机器人在超前执行命令过程中的碰撞状态，碰撞检测采用两阶段策略，第一阶段使用边界盒预检，第二阶段使用Physics.ComputePenetration精确计算；

命令缓冲模块，用于延迟命令执行以实现超前验证，命令缓冲模块存储目标命令并根据超前时间窗口控制命令的下发时机；

安全决策模块，用于根据碰撞检测结果生成安全决策，安全决策类型包括允许通行、限速通行和阻断通行，并根据安全决策控制是否将命令下发至真实机器人。

### 从属权利要求

**3.** 根据权利要求1所述的方法，其特征在于，步骤S4中所述碰撞检测还包括连杆关键点检测，通过运动学计算获取机械臂的关节位置、连杆中点和末端执行器的世界坐标位置，对每个关键点使用OverlapSphere检测是否与障碍物重叠。

**4.** 根据权利要求1所述的方法，其特征在于，还包括奇异性检测步骤：

计算影子机械臂执行命令过程中的雅可比矩阵的角速度部分；

计算雅可比矩阵角速度部分与其转置的乘积的行列式作为Gram行列式；

当Gram行列式小于硬阈值时生成阻断决策，当Gram行列式小于软阈值但大于硬阈值时生成限速决策，其中硬阈值为1.0×10⁻⁴，软阈值为2.5×10⁻⁴。

**5.** 根据权利要求1所述的方法，其特征在于，还包括负载感知的安全边界膨胀步骤：

获取机器人的当前负载状态；

根据负载状态计算膨胀系数，膨胀系数计算公式为：effectiveInflation = baseInflation × (1 + loadFactor × loadInflationGain)，其中baseInflation为基础膨胀量，loadFactor为负载系数，loadInflationGain为负载膨胀增益，默认值为0.35；

根据膨胀系数调整碰撞检测的安全阈值。

**6.** 根据权利要求1所述的方法，其特征在于，超前时间窗口的动态计算方法包括：

计算基础窗口为通信延迟与计算预算之和与最小超前时间的较大值；

当为底盘命令时，计算制动距离时间为当前速度除以制动减速度；

当为机械臂命令时，计算制动时间为最大关节速度除以关节制动减速度；

最终超前时间为基础窗口、制动距离时间和最大关节速度制动时间的最大值。

**7.** 根据权利要求2所述的系统，其特征在于，影子孪生模块包括：

底盘影子孪生子模块，用于克隆差速驱动底盘并管理底盘命令的超前验证，底盘影子孪生子模块创建底盘的完整层级克隆体并同步运动学参数、物理参数和控制器参数；

机械臂影子孪生子模块，用于克隆多自由度机械臂并管理关节命令的超前验证，机械臂影子孪生子模块通过路径映射机制建立真实关节与影子关节的对应关系并复制关节控制器和驱动参数。

**8.** 根据权利要求2所述的系统，其特征在于，安全决策模块包括：

轨迹采样单元，用于根据命令起点和终点沿轨迹生成采样点序列，采样间距为0.08米，最大采样点数为64；

重叠检测单元，用于对采样点进行OverlapSphere快速碰撞筛选；

精确检测单元，用于对通过重叠检测的采样点进行ComputePenetration精确穿透计算；

决策生成单元，用于根据穿透深度和净空计算结果生成Allow、Throttle或Block决策。

**9.** 根据权利要求1所述的方法，其特征在于，安全决策的净空阈值判断规则为：

当最小净空小于阻断净空阈值0.01米时，生成阻断决策；

当最小净空小于限速净空阈值0.05米但大于阻断净空阈值时，生成限速决策；

限速比例根据当前净空在阻断阈值和限速阈值之间的位置线性插值计算，范围为0.25至0.85。

**10.** 根据权利要求1所述的方法，其特征在于，还包括死区检测步骤，判断目标位置或轨迹是否使机械臂末端进入可达死区，当检测到死区风险时生成阻断决策。

---

# 摘要

本发明公开了一种基于影子孪生的预测性安全防护方法及系统。该方法通过创建物理隔离的影子机器人副本来实现主动式安全防护。影子机器人与真实机器人共享相同的运动学参数、物理参数和控制器参数，在超前时间窗口内超前执行命令。在超前执行过程中，系统通过两阶段碰撞检测精确检测影子机器人与障碍物的碰撞状态，通过雅可比矩阵Gram行列式检测机械臂奇异性风险，并根据负载状态动态调整安全边界。系统根据检测结果生成允许通行、限速通行或阻断通行三种决策，只有通过安全验证的命令才会被下发至真实机器人执行。本发明能够在碰撞发生前预测并阻止危险命令，实现主动式安全防护，适用于移动底盘、机械臂及移动操作机器人等多种类型的机器人系统。

---

_文档版本: v3.0_
_生成时间: 2026-05-09_
_基于项目: wheel (Unity 2022.3.52f1c1)_
_改进说明: 增加具体场景和数值，消除AI味_
