# 专利申请文档

## 一种基于影子孪生的预测性安全防护方法及系统

---

# 说明书

## 第一章 技术领域

本发明涉及机器人技术领域，具体涉及一种基于影子孪生的预测性安全防护方法及系统，尤其适用于具有移动底盘和机械臂的移动操作机器人的安全控制。

## 第二章 背景技术

### 2.1 传统机器人安全防护的局限性

随着工业自动化和服务机器人的快速发展，机器人在复杂环境中的安全运行变得越来越重要。传统的机器人安全防护方法主要依赖以下几种方式：

1. **紧急停止开关**：当检测到碰撞时立即停止所有运动，但无法预防碰撞的发生。

2. **接触力检测**：通过力矩传感器检测碰撞接触力来触发停止响应，但反应时间取决于传感器灵敏度和控制系统的响应速度。

3. **碰撞检测后处理**：在碰撞发生后通过检测碰撞信号来停止或反转运动，但此时碰撞已经发生，无法避免物理损伤。

这些传统方法存在共同的局限性：它们都是**被动响应式**的安全措施，在碰撞发生之后才能发挥作用，无法在碰撞发生前预测并阻止危险命令的执行。

### 2.2 现有预测性防护方法的不足

为解决传统方法的不足，研究者提出了多种预测性安全防护方法，主要包括：

1. **基于传感器融合的避障**：利用视觉、激光雷达等环境感知传感器检测障碍物，但存在感知盲区和计算延迟。

2. **基于速度限制的安全区域**：在机器人周围设定安全区域，当检测到人员或障碍物接近时降低速度，但无法精确预测碰撞发生时刻。

3. **基于规则的安全约束**：通过预定义的安全规则限制机器人的运动范围和速度，但缺乏灵活性，难以适应复杂多变的作业环境。

这些方法虽然能够在一定程度上提高安全性，但仍存在以下不足：

- 预测能力有限，无法精确预测机器人未来运动轨迹与障碍物的相互作用
- 对复杂环境的适应性差，特别是在存在动态障碍物的场景中
- 难以处理机械臂的奇异性问题和关节空间中的碰撞风险

### 2.3 影子孪生概念的引入

为解决上述问题，本发明提出了一种全新的**影子孪生（Shadow Twin）**概念。影子孪生是指在虚拟空间中创建一个与真实机器人物理隔离的虚拟副本，通过让该虚拟副本超前执行目标命令来验证命令的安全性，从而在危险发生前阻止不安全的命令下发到真实机器人。

与传统的仿真验证方法不同，影子孪生具有以下特点：

- **物理真实性**：影子机器人保持与真实机器人相同的物理参数（质量、惯性矩、摩擦系数等），确保运动行为的一致性
- **时间超前性**：影子机器人超前执行命令的时间窗口足以完成安全验证，通常设置为0.35秒
- **碰撞隔离性**：影子机器人与真实机器人的碰撞检测系统相互独立，影子中的碰撞不会触发真实机器人的保护机制

## 第三章 发明概述

### 3.1 发明目的

本发明的目的是提供一种基于影子孪生的预测性安全防护方法及系统，以解决现有技术中存在的以下问题：

1. 被动响应式安全防护无法预防碰撞发生
2. 预测能力有限，无法精确预测复杂环境中的碰撞风险
3. 机械臂奇异性检测和关节空间碰撞检测不完善
4. 安全策略缺乏灵活性，难以适应不同负载和工况

### 3.2 技术方案概述

本发明采用以下技术方案来实现上述目的：

**一种基于影子孪生的机器人安全防护方法，包括以下步骤：**

S1. 创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享相同的运动学参数、物理参数、控制器参数和关节限位配置；

S2. 获取真实机器人当前状态的镜像快照，包括位置、速度、关节角度等信息；

S3. 将目标命令同时发送到影子机器人和命令缓冲队列，影子机器人在超前时间窗口内超前执行该命令；

S4. 在影子机器人执行命令的过程中，实时检测影子机器人与预设障碍物之间的碰撞状态；

S5. 根据碰撞检测结果生成安全决策，决策类型包括允许通行、限速通行和阻断通行；

S6. 若安全决策为允许通行或限速通行，则在超前时间窗口结束后将命令下发至真实机器人；若决策为阻断通行，则阻止命令下发并触发安全警报。

**一种基于影子孪生的机器人安全防护系统，包括：**

- 影子孪生模块，用于克隆真实机器人并超前执行命令
- 碰撞检测模块，用于检测影子机器人的碰撞状态
- 命令缓冲模块，用于延迟命令执行以实现超前验证
- 安全决策模块，根据检测结果生成Allow/Throttle/Block决策

### 3.3 技术效果

与现有技术相比，本发明具有以下技术效果：

1. **预测性防护**：通过影子孪生超前执行命令，能够在碰撞发生前预测并阻止危险命令，实现主动式安全防护。

2. **精确检测**：采用Physics.ComputePenetration精确计算碰撞穿透深度，检测精度远高于传统的重叠检测方法。

3. **多级安全策略**：通过Allow/Throttle/Block三级安全决策，在安全性和效率之间取得平衡。

4. **奇异性保护**：基于雅可比矩阵行列式的奇异性检测，能够提前识别机械臂的奇异构型并采取保护措施。

5. **负载适应性**：安全边界根据机器人负载状态动态膨胀，适应不同工况下的安全需求。

6. **通用性强**：影子孪生架构可应用于移动底盘、机械臂及移动操作机器人等多种类型的机器人系统。

## 第四章 核心定义与数据模型

### 4.1 影子孪生相关定义

**定义4.1.1 影子机器人（Shadow Robot）**

影子机器人是指根据真实机器人克隆而成的虚拟机器人副本，其具备以下特征：

- 与真实机器人共享相同的层级结构
- 保持与真实机器人一致的物理参数
- 具有独立的碰撞检测系统
- 运行在超前时间窗口内执行命令

**定义4.1.2 镜像快照（Mirror Snapshot）**

镜像快照是描述真实机器人当前状态的数据结构，包括：

```
MirrorSnapshot {
    Vector3 baseWorldPosition;           // 底盘世界位置
    Quaternion baseWorldRotation;          // 底盘世界旋转
    float basePlanarSpeed;               // 底盘平面速度
    float baseYawRate;                   // 底盘偏航角速度
    float[] armJointAnglesDeg;           // 机械臂关节角度（度）
    float[] armJointVelocitiesDegPerSecond; // 机械臂关节角速度（度/秒）
    Vector3 armEndEffectorWorldPosition;   // 末端执行器世界位置
}
```

**定义4.1.3 超前时间（Lead Time）**

超前时间是指影子机器人超前执行命令的时间间隔，默认设置为0.35秒。该时间确保有足够的时间完成安全验证，并在危险发生时能够在真实机器人执行命令前阻止。

### 4.2 安全决策类型

**定义4.2.1 安全决策类型（SafetyGateDecisionType）**

```
enum SafetyGateDecisionType {
    Allow,      // 允许通行：命令安全，按原速执行
    Throttle,   // 限速通行：存在安全风险，按throttleRatio限速执行
    Block       // 阻断通行：存在严重危险，阻止命令执行
}
```

**定义4.2.2 阻断分类（SafetyGateBlockCategory）**

```
enum SafetyGateBlockCategory {
    None,                              // 无阻断
    GateBlockedByCollision,            // 碰撞阻断：预测到碰撞发生
    GateBlockedByDeadZone,             // 死区阻断：目标位于机械臂可达死区内
    GateBlockedByClearance,             // 净空不足阻断：预测净空小于安全阈值
    GateBlockedBySingularity            // 奇异性阻断：预测到奇异构型
}
```

### 4.3 预测状态结构

**定义4.3.1 预测状态（ShadowPredictorState）**

```
ShadowPredictorState {
    Vector3 predictedBaseWorldPosition;     // 预测底盘世界位置
    Quaternion predictedBaseWorldRotation;  // 预测底盘世界旋转
    float[] predictedArmJointAnglesDeg;   // 预测机械臂关节角度
    float predictedLeadTimeSeconds;        // 实际超前时间（秒）
    float minPredictedClearanceMeters;     // 预测最小净空（米）
}
```

### 4.4 运行时上下文

**定义4.4.1 安全门控运行时上下文（SafetyGateRuntimeContext）**

```
SafetyGateRuntimeContext {
    bool enabled;                              // 门控使能
    bool throttleOnRisk;                       // 风险时是否限速
    float commLatencySeconds;                   // 通信延迟（默认0.04s）
    float computeBudgetSeconds;                  // 计算预算（默认0.02s）
    float minLookaheadSeconds;                 // 最小超前时间（默认0.08s）
    float baseInflationMeters;                 // 底盘安全膨胀量（默认0.02m）
    float armInflationMeters;                  // 机械臂安全膨胀量（默认0.015m）
    float baseRadiusMeters;                    // 底盘等效半径（默认0.35m）
    float loadFactor;                          // 负载系数
    float loadInflationGain;                   // 负载膨胀增益（默认0.35）
    float baseBrakeDecelMetersPerSecond2;     // 底盘制动减速度（默认0.9m/s²）
    float armBrakeDecelDegPerSecond2;         // 机械臂制动减速度（默认120°/s²）
    float gateBlockClearanceMeters;            // 阻断净空阈值（默认0.01m）
    float gateThrottleClearanceMeters;        // 限速净空阈值（默认0.05m）
    float deadZoneMarginMeters;                // 死区边界余量（默认0.01m）
    float queueLeadTimeSeconds;               // 队列超前时间
    int maxRecoveryReplans;                  // 最大重规划次数（默认3）
}
```

## 第五章 影子孪生系统架构

### 5.1 系统架构概述

影子孪生系统由以下核心组件构成：

```
┌─────────────────────────────────────────────────────────────┐
│                  RobotSimulationManager                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌───────────────┐  │
│  │ ShadowBaseTwin │  │ ShadowArmTwin  │  │   Shadow     │  │
│  │   Runtime      │  │   Runtime      │  │RobotVisualizer│  │
│  │  (底盘影子)    │  │  (机械臂影子)  │  │  (可视化)    │  │
│  └────────┬──────┘  └────────┬──────┘  └───────┬───────┘  │
│           │                    │                   │          │
│           └────────────────────┼───────────────────┘          │
│                                ▼                              │
│              ┌─────────────────────────┐                    │
│              │   ShadowSimulationGate   │                    │
│              │      (安全门控)         │                    │
│              └───────────┬─────────────┘                    │
│                          │                                  │
│           ┌───────────────┼───────────────┐                  │
│           ▼               ▼               ▼                  │
│    ┌────────────┐  ┌──────────────┐  ┌──────────────┐        │
│    │  DiffDrive │  │   Arm6DOF   │  │ Coordinated │        │
│    │  Twin      │  │IKController │  │TaskPlanner   │        │
│    └────────────┘  └──────────────┘  └──────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 影子底盘孪生运行时（ShadowBaseTwinRuntime）

#### 5.2.1 功能概述

影子底盘孪生运行时负责创建和管理底盘的影子副本，其核心职责包括：

- 创建物理隔离的底盘克隆体
- 同步控制器参数和物理参数
- 管理延迟命令队列
- 执行超前验证检测

#### 5.2.2 物理克隆机制

影子底盘通过Unity的Object.Instantiate方法完整复制真实底盘的层级结构：

```csharp
// 克隆真实底盘
_shadowRootObject = Object.Instantiate(_liveSourceRoot.gameObject);
_shadowRootObject.name = $"Preview_ShadowBaseTwin_{_liveSourceRoot.name}";
_shadowRootObject.hideFlags = HideFlags.DontSave;
```

关键实现要点：

1. **层级结构完整复制**：包括所有子物体、Transform层级、Collider等
2. **HideFlags设置**：确保影子对象不被序列化保存
3. **控制器组件获取**：通过GetComponentInChildren获取影子控制器

#### 5.2.3 控制器参数同步

为确保影子底盘与真实底盘运动行为一致，需要同步以下控制器参数：

```csharp
private void SyncShadowControllerParametersFromLive()
{
    // 运动学参数
    _shadowController.vMax = _liveController.vMax;           // 最大线速度
    _shadowController.aMax = _liveController.aMax;           // 最大线加速度
    _shadowController.wMax = _liveController.wMax;         // 最大角速度
    _shadowController.alphaMax = _liveController.alphaMax;   // 最大角加速度

    // 增益参数
    _shadowController.kDist = _liveController.kDist;         // 距离增益
    _shadowController.kYaw = _liveController.kYaw;         // 航向增益

    // 相位转换参数
    _shadowController.rotateInPlaceAngleDeg = _liveController.rotateInPlaceAngleDeg;
    _shadowController.posTolerance = _liveController.posTolerance;
    _shadowController.arrivalSnapDistance = _liveController.arrivalSnapDistance;
    _shadowController.brakingDistancePadding = _liveController.brakingDistancePadding;

    // ... 共同步28个控制参数
}
```

#### 5.2.4 物理参数同步

```csharp
private void SyncShadowPhysicsFromLive()
{
    Rigidbody liveRb = _liveController.rb;
    Rigidbody shadowRb = _shadowController.rb;

    shadowRb.mass = liveRb.mass;                              // 质量
    shadowRb.drag = liveRb.drag;                               // 线性阻力
    shadowRb.angularDrag = liveRb.angularDrag;                // 角阻力
    shadowRb.useGravity = liveRb.useGravity;                   // 重力
    shadowRb.isKinematic = liveRb.isKinematic;                 // 运动学模式
    shadowRb.interpolation = liveRb.interpolation;             // 插值模式
    shadowRb.collisionDetectionMode = liveRb.collisionDetectionMode; // 碰撞检测模式
    shadowRb.constraints = liveRb.constraints;                 // 约束
    shadowRb.centerOfMass = liveRb.centerOfMass;              // 质心位置
    shadowRb.inertiaTensor = liveRb.inertiaTensor;            // 惯性张量
    shadowRb.inertiaTensorRotation = liveRb.inertiaTensorRotation; // 惯性张量旋转
}
```

#### 5.2.5 碰撞隔离机制

影子底盘与真实底盘之间的碰撞隔离通过Physics.IgnoreCollision实现：

```csharp
private void IgnoreLiveShadowCollisions()
{
    for (int liveIndex = 0; liveIndex < _liveRobotColliders.Count; liveIndex++)
    {
        for (int shadowIndex = 0; shadowIndex < _shadowBaseColliders.Count; shadowIndex++)
        {
            Physics.IgnoreCollision(
                _liveRobotColliders[liveIndex],
                _shadowBaseColliders[shadowIndex],
                true);  // 忽略碰撞
        }
    }
}
```

#### 5.2.6 延迟命令队列

延迟命令队列是实现超前执行的核心机制：

```csharp
internal struct ShadowBaseTwinDelayedCommand
{
    public ShadowBaseTwinCommandType type;      // 命令类型
    public float executeRealtime;              // 执行时间戳
    public float linearVelocity;               // 线速度
    public float angularVelocity;               // 角速度
    public Vector3 vectorValue;                  // 位置/目标点
    public float floatValue;                    // 偏航角

    // 工厂方法
    public static ShadowBaseTwinDelayedCommand CreateVelocity(
        float executeRealtime,
        float linearVelocity,
        float angularVelocity,
        Vector3 trackingPoint)
    {
        return new ShadowBaseTwinDelayedCommand {
            type = ShadowBaseTwinCommandType.Velocity,
            executeRealtime = executeRealtime,
            linearVelocity = linearVelocity,
            angularVelocity = angularVelocity,
            vectorValue = trackingPoint
        };
    }
}

// 入队示例
EnqueueLiveReplica(ShadowBaseTwinDelayedCommand.CreateTargetPoint(
    Time.realtimeSinceStartup + _leadSeconds,  // 0.35秒后执行
    point));
```

### 5.3 影子机械臂孪生运行时（ShadowArmTwinRuntime）

#### 5.3.1 功能概述

影子机械臂孪生运行时负责创建和管理机械臂的影子副本，其核心职责包括：

- 创建物理隔离的机械臂克隆体
- 复制6个关节的ArticulationBody和梯形轨迹控制器
- 同步关节驱动参数
- 执行机械臂运动验证

#### 5.3.2 关节控制器复制

```csharp
private void CollectJointMappingsAndControllers()
{
    for (int jointIndex = 0; jointIndex < 6; jointIndex++)
    {
        // 获取真实关节
        ArticulationBody liveJoint = _liveController.joints[jointIndex];

        // 计算相对路径并在影子中查找对应关节
        string relativePath = GetRelativePath(_liveArmRoot, liveJoint.transform);
        Transform shadowJointTransform = _shadowRootObject.transform.Find(relativePath);
        ArticulationBody shadowJoint = shadowJointTransform.GetComponent<ArticulationBody>();

        // 克隆梯形轨迹控制器
        OneJointTrapezoidController shadowControllerComponent =
            shadowJointTransform.gameObject.AddComponent<OneJointTrapezoidController>();
        shadowControllerComponent.joint = shadowJoint;
        CopyJointControllerConfiguration(liveControllerComponent, shadowControllerComponent);

        _shadowJoints.Add(shadowJoint);
        _shadowJointControllers.Add(shadowControllerComponent);
    }
}
```

#### 5.3.3 驱动参数同步

```csharp
private void SyncShadowConfigurationFromLive()
{
    for (int i = 0; i < 6; i++)
    {
        ArticulationDrive liveDrive = liveJoint.xDrive;
        ArticulationDrive shadowDrive = shadowJoint.xDrive;

        // 关节限位
        shadowDrive.lowerLimit = liveDrive.lowerLimit;
        shadowDrive.upperLimit = liveDrive.upperLimit;

        // 驱动特性
        shadowDrive.stiffness = liveDrive.stiffness;
        shadowDrive.damping = liveDrive.damping;
        shadowDrive.forceLimit = liveDrive.forceLimit;

        shadowJoint.xDrive = shadowDrive;

        // 同步梯形控制器参数
        CopyJointControllerConfiguration(liveControllerComponent, shadowControllerComponent);
    }
}
```

#### 5.3.4 机械臂碰撞检测

```csharp
private bool TryDetectShadowArmCollision(out string collisionMessage)
{
    collisionMessage = string.Empty;

    for (int shadowIndex = 0; shadowIndex < _shadowArmColliders.Count; shadowIndex++)
    {
        Collider shadowCollider = _shadowArmColliders[shadowIndex];

        for (int obstacleIndex = 0; obstacleIndex < _activeObstacles.Count; obstacleIndex++)
        {
            Collider obstacle = _activeObstacles[obstacleIndex];

            // 边界盒预检
            if (!shadowCollider.bounds.Intersects(obstacle.bounds))
                continue;

            // 精确穿透计算
            if (!Physics.ComputePenetration(
                shadowCollider, shadowCollider.transform.position, shadowCollider.transform.rotation,
                obstacle, obstacle.transform.position, obstacle.transform.rotation,
                out Vector3 direction, out float distance))
                continue;

            // 穿透阈值判断
            if (distance <= GetEffectivePenetrationThreshold(shadowCollider, obstacle))
                continue;

            collisionMessage = $"Shadow arm collided with obstacle '{obstacle.name}'...";
            return true;
        }
    }

    return false;
}
```

### 5.4 影子机器人可视化器（ShadowRobotVisualizer）

#### 5.4.1 三种预览模式

```csharp
private enum PreviewMode
{
    Mirror,       // 镜像模式：实时跟随真实机器人
    BasePreview,  // 底盘预览：预测底盘运动轨迹
    ArmPreview    // 机械臂预览：预测机械臂运动轨迹
}
```

#### 5.4.2 预测位姿推送

```csharp
public void PushPredictedBasePose(Vector3 worldPosition, Quaternion worldRotation, float holdSeconds)
{
    _predictedBasePosition = worldPosition;
    _predictedBaseRotation = worldRotation;
    _predictedBasePoseExpireRealtime = Time.realtimeSinceStartup + Mathf.Max(0.05f, holdSeconds);
    _hasPredictedBasePose = true;
}
```

#### 5.4.3 预测位姿积分

```csharp
private static void IntegrateBasePose(
    ref Vector3 worldPosition,
    ref Quaternion worldRotation,
    float linearVelocity,
    float angularVelocity,
    float deltaTime)
{
    Vector3 planarForward = planarRotation * Vector3.forward;

    if (Mathf.Abs(angularVelocity) <= 1e-4f)
    {
        // 直线运动
        worldPosition = startPosition + (planarForward * linearVelocity * deltaTime);
    }
    else
    {
        // 圆弧运动
        float deltaYawRad = angularVelocity * deltaTime;
        float radius = linearVelocity / angularVelocity;

        Vector3 planarDisplacement =
            (planarForward * (radius * Mathf.Sin(deltaYawRad)))
            + (planarRight * (radius * (1f - Mathf.Cos(deltaYawRad))));

        worldPosition = startPosition + planarDisplacement;
    }
}
```

## 第六章 安全门控与决策

### 6.1 安全门控核心组件（ShadowSimulationGate）

安全门控是整个预测性安全防护系统的核心决策单元，负责：

- 评估底盘命令的轨迹安全性
- 评估机械臂命令的轨迹安全性
- 生成Allow/Throttle/Block决策
- 执行轨迹验证和路径验证

### 6.2 底盘命令评估

```csharp
public SafetyGateDecision EvaluateBaseCommand(
    MirrorSnapshot snapshot,
    BaseGateCommand command,
    SafetyGateRuntimeContext context)
{
    SafetyGateDecision decision = CreateDefaultGateDecision(...);

    // 1. 计算采样数量（基于距离和超前时间）
    int previewSampleCount = EstimateBasePreviewSampleCount(
        command.fromWorldPosition,
        command.toWorldPosition,
        basePreviewSampleSpacingMeters,  // 0.08m
        decision.predictorState.predictedLeadTimeSeconds);

    bool collisionDetected = false;
    Collider blockingCollider = null;
    float previewWorstClearance = float.PositiveInfinity;

    // 2. 沿轨迹采样检测
    for (int sampleIndex = 0; sampleIndex <= previewSampleCount; sampleIndex++)
    {
        float t = sampleIndex / (float)previewSampleCount;
        Vector3 samplePosition = Vector3.Lerp(
            command.fromWorldPosition,
            command.toWorldPosition,
            t);

        // 重叠检测
        if (TryOverlapObstacle(samplePosition, inflatedBaseRadius, context.obstacles,
            context.obstacleLookup, out Collider sampleHitCollider))
        {
            collisionDetected = true;
            blockingCollider = sampleHitCollider;
            break;
        }

        // 净空计算
        float sampleClearance = ComputeSphereClearance(samplePosition, inflatedBaseRadius, context.obstacles);
        if (sampleClearance < previewWorstClearance)
            previewWorstClearance = sampleClearance;
    }

    // 3. 决策生成
    if (collisionDetected)
    {
        decision.type = SafetyGateDecisionType.Block;
        decision.blockCategory = SafetyGateBlockCategory.GateBlockedByCollision;
        return decision;
    }

    // 4. 净空阈值判断
    float blockClearance = Mathf.Max(0.001f, context.gateBlockClearanceMeters);
    float throttleClearance = Mathf.Max(blockClearance, context.gateThrottleClearanceMeters);

    if (minClearance <= blockClearance)
    {
        decision.type = SafetyGateDecisionType.Block;
        decision.blockCategory = SafetyGateBlockCategory.GateBlockedByClearance;
    }
    else if (minClearance <= throttleClearance)
    {
        decision.type = SafetyGateDecisionType.Throttle;
        decision.throttleRatio = ComputeThrottleRatio(minClearance, blockClearance, throttleClearance);
    }

    return decision;
}
```

### 6.3 机械臂命令评估

```csharp
public SafetyGateDecision EvaluateArmCommand(
    MirrorSnapshot snapshot,
    ArmGateCommand command,
    SafetyGateRuntimeContext context,
    Arm6DOFFKController armController)
{
    // 1. 碰撞守卫评估
    if (armController.EvaluateMotionCollision(fromAngles, command.toAnglesDeg,
        out ArmCollisionGuardResult guardResult))
    {
        decision.type = SafetyGateDecisionType.Block;
        decision.blockCategory = SafetyGateBlockCategory.GateBlockedByCollision;
        return decision;
    }

    // 2. 沿关节轨迹采样
    for (int sampleIndex = 0; sampleIndex <= previewSampleCount; sampleIndex++)
    {
        float t = sampleIndex / (float)previewSampleCount;
        float[] sampleAngles = LerpAngles(fromAngles, command.toAnglesDeg, t);

        // 3. 雅可比行列式计算（奇异性检测）
        float[,] sampleJacobian = armController.ComputeGeometricJacobian(sampleAngles);
        float sampleDeterminant = ComputeLinearJacobianGramDeterminant(sampleJacobian);

        if (sampleDeterminant <= armSingularityDeterminantHardThreshold)
        {
            decision.type = SafetyGateDecisionType.Block;
            decision.blockCategory = SafetyGateBlockCategory.GateBlockedBySingularity;
            return decision;
        }

        // 4. 连杆关键点碰撞检测
        Pose[] sampleLinkPoses = armController.ComputeLinkPosesWorld(sampleAngles);
        for (int poseIndex = 0; poseIndex < sampleLinkPoses.Length; poseIndex++)
        {
            Vector3 point = sampleLinkPoses[poseIndex].position;

            if (TryOverlapObstacle(point, inflatedArmRadius, context.obstacles,
                context.obstacleLookup, out _))
            {
                decision.type = SafetyGateDecisionType.Block;
                decision.blockCategory = SafetyGateBlockCategory.GateBlockedByCollision;
                return decision;
            }
        }
    }

    // 5. 奇异性限速判断
    if (minPreviewDeterminant <= armSingularityDeterminantThrottleThreshold)
    {
        decision.type = SafetyGateDecisionType.Throttle;
        decision.throttleRatio = ComputeThrottleRatioBySingularity(
            minPreviewDeterminant,
            armSingularityDeterminantHardThreshold,
            armSingularityDeterminantThrottleThreshold);
    }

    return decision;
}
```

### 6.4 雅可比奇异性检测

```csharp
private static float ComputeLinearJacobianGramDeterminant(float[,] jacobian)
{
    // 提取线性雅可比部分 (3x6)
    float[,] jjt = new float[3, 3];
    for (int row = 0; row < 3; row++)
    {
        for (int column = 0; column < 3; column++)
        {
            float sum = 0f;
            for (int jointIndex = 0; jointIndex < 6; jointIndex++)
            {
                sum += jacobian[3 + row, jointIndex] * jacobian[3 + column, jointIndex];
            }
            jjt[row, column] = sum;
        }
    }

    // 计算3x3矩阵行列式
    float determinant =
        (jjt[0, 0] * ((jjt[1, 1] * jjt[2, 2]) - (jjt[1, 2] * jjt[2, 1])))
        - (jjt[0, 1] * ((jjt[1, 0] * jjt[2, 2]) - (jjt[1, 2] * jjt[2, 0])))
        + (jjt[0, 2] * ((jjt[1, 0] * jjt[2, 1]) - (jjt[1, 1] * jjt[2, 0])));

    return Mathf.Max(0f, determinant);
}
```

### 6.5 限速比例计算

```csharp
private static float ComputeThrottleRatio(
    float minClearance,
    float blockThreshold,
    float throttleThreshold)
{
    // 线性插值计算限速比例
    float normalized = Mathf.InverseLerp(blockThreshold, throttleThreshold, minClearance);
    return Mathf.Clamp(Mathf.Lerp(0.25f, 0.85f, normalized), 0.2f, 0.9f);
}

private static float ComputeThrottleRatioBySingularity(
    float determinant,
    float hardThreshold,
    float throttleThreshold)
{
    float normalized = Mathf.InverseLerp(hardThreshold, throttleThreshold, determinant);
    return Mathf.Clamp(Mathf.Lerp(0.2f, 0.85f, normalized), 0.2f, 0.9f);
}
```

### 6.6 超前时间计算

```csharp
private static float ComputeLookaheadSeconds(SafetyGateRuntimeContext context, MirrorSnapshot snapshot, bool armMode)
{
    // 基础窗口：通信延迟 + 计算预算
    float baseWindow = Mathf.Max(
        context.minLookaheadSeconds,
        context.commLatencySeconds + context.computeBudgetSeconds);

    if (!armMode)
    {
        // 底盘：取制动距离对应时间
        float baseBrake = snapshot.basePlanarSpeed / context.baseBrakeDecelMetersPerSecond2;
        return Mathf.Max(baseWindow, baseBrake);
    }
    else
    {
        // 机械臂：取最大关节速度对应制动时间
        float maxJointSpeed = GetMaxJointSpeed(snapshot.armJointVelocitiesDegPerSecond);
        float armBrake = maxJointSpeed / context.armBrakeDecelDegPerSecond2;
        return Mathf.Max(baseWindow, armBrake);
    }
}
```

## 第七章 命令时间线缓冲系统

### 7.1 时间线缓冲结构

```csharp
public sealed class SafetyGateTimelineBuffer
{
    private readonly Queue<SafetyGateTimelineCommand> _queue = new Queue<SafetyGateTimelineCommand>();
    private readonly object _sync = new object();

    public int Count { get; }

    public void Enqueue(SafetyGateTimelineCommand command)
    {
        lock (_sync)
        {
            _queue.Enqueue(command);
        }
    }

    public bool TryDequeueReady(float nowRealtime, float requiredLeadSeconds,
        out SafetyGateTimelineCommand command)
    {
        lock (_sync)
        {
            if (_queue.Count <= 0)
            {
                command = default;
                return false;
            }

            SafetyGateTimelineCommand head = _queue.Peek();
            float requiredReadyRealtime = head.enqueueRealtime + Mathf.Max(0f, requiredLeadSeconds);
            float headReadyRealtime = head.readyRealtime > 0f
                ? Mathf.Max(requiredReadyRealtime, head.readyRealtime)
                : requiredReadyRealtime;

            if (nowRealtime + 1e-5f < headReadyRealtime)
            {
                command = default;
                return false;
            }

            command = _queue.Dequeue();
            return true;
        }
    }
}
```

### 7.2 命令序列号追踪

每个命令携带唯一的序列号，用于追踪和调试：

```csharp
public struct SafetyGateTimelineCommand
{
    public long sequenceId;                    // 命令序列号
    public float enqueueRealtime;             // 入队时间
    public float readyRealtime;              // 就绪时间
    public SafetyGateTimelineCommandKind kind; // 命令类型（Base/Arm）
    public BaseGateCommand baseCommand;       // 底盘命令
    public ArmGateCommand armCommand;         // 机械臂命令
    public float predictedLeadSeconds;        // 预测超前时间
    public string predictedRiskSummary;       // 风险摘要
}
```

## 第八章 关键算法

### 8.1 穿透深度计算

Physics.ComputePenetration是Unity提供的精确碰撞穿透计算API：

```csharp
bool overlapping = Physics.ComputePenetration(
    collider1,                     // 第一个碰撞体
    position1,                      // 第一个碰撞体世界位置
    rotation1,                      // 第一个碰撞体世界旋转
    collider2,                     // 第二个碰撞体
    position2,                      // 第二个碰撞体世界位置
    rotation2,                      // 第二个碰撞体世界旋转
    out Vector3 direction,          // 穿透方向（从collider2指向collider1）
    out float distance              // 穿透深度
);
```

### 8.2 有效穿透阈值

```csharp
private static float GetEffectivePenetrationThreshold(Collider primary, Collider secondary)
{
    return Mathf.Max(0.0001f,
        GetSafeContactOffset(primary) + GetSafeContactOffset(secondary));
}

private static float GetSafeContactOffset(Collider collider)
{
    return collider != null ? Mathf.Max(0f, collider.contactOffset) : 0f;
}
```

### 8.3 安全膨胀计算

```csharp
private static float ComputeInflation(float baseInflation, SafetyGateRuntimeContext context)
{
    if (context == null)
        return Mathf.Max(0f, baseInflation);

    float safeBaseInflation = Mathf.Max(0f, baseInflation);
    float scale = 1f + Mathf.Max(0f, context.loadFactor) * Mathf.Max(0f, context.loadInflationGain);
    return safeBaseInflation * scale;
}
```

## 第九章 安全策略

### 9.1 三级安全响应机制

| 决策类型 | 条件                               | 响应动作                       |
| -------- | ---------------------------------- | ------------------------------ |
| Allow    | 预测无碰撞，净空充足，无奇异性风险 | 命令按原速原样执行             |
| Throttle | 预测净空不足或接近奇异性阈值       | 命令按throttleRatio限速执行    |
| Block    | 预测碰撞发生、净空不足或奇异       | 命令被阻止，不下发至真实机器人 |

### 9.2 负载感知的安全膨胀

安全边界根据机器人负载状态动态调整：

```
effectiveInflation = baseInflation × (1 + loadFactor × loadInflationGain)
```

当负载增加时，安全边界相应膨胀，提供更大的安全裕度。

### 9.3 异常恢复机制

```csharp
public void FailSession(string reason)
{
    if (HasFault)  // 已有故障则不重复处理
        return;

    _faultMessage = reason;
    Debug.LogError($"[ShadowTwinRuntime] {reason}");
    StopAllMotion();
}

public void CompleteSession(bool resetShadowToLivePose)
{
    _sessionActive = false;
    _sessionLabel = string.Empty;
    _faultMessage = string.Empty;
    _liveCommandQueue.Clear();

    if (resetShadowToLivePose)
        ResetShadowToLivePose();
}
```

## 第十章 实施方式

### 10.1 移动机器人底盘实施例

本实施例展示影子孪生技术在差速驱动移动底盘上的应用：

**场景描述**：

- 移动底盘在仓库环境中运动，需要躲避静态障碍物和动态行人
- 底盘需要响应操作员的点目标指令，精确移动到指定位置

**实施步骤**：

S1. 当操作员点击设置目标点时，命令同时发送到影子底盘和命令缓冲队列

S2. 影子底盘在0.35秒超前时间窗口内超前执行该目标点指令

S3. 在超前执行过程中，安全门控沿轨迹采样检测碰撞风险

S4. 若检测到障碍物，生成Throttle或Block决策

S5. 若决策为允许，在超前时间结束后将命令下发至真实底盘

S6. 若决策为阻断，阻止命令下发并提示操作员障碍物位置

### 10.2 机械臂实施例

本实施例展示影子孪生技术在6-DOF机械臂上的应用：

**场景描述**：

- 机械臂需要从托盘中抓取工件
- 工件周围存在其他障碍物，需要精确规划运动路径
- 机械臂需要避开奇异构型，保证运动平滑性

**实施步骤**：

S1. IK求解器计算目标工件的关节角度解

S2. 将目标关节角度同时发送到影子机械臂

S3. 影子机械臂在超前时间窗口内执行关节空间轨迹

S4. 在执行过程中进行以下检测：

- 碰撞检测：沿轨迹采样检测与障碍物的重叠
- 奇异性检测：计算雅可比行列式判断是否接近奇异位置
- 死区检测：判断末端是否进入可达死区

S5. 根据检测结果生成安全决策

S6. 若决策为允许，在超前时间结束后将命令下发至真实机械臂

### 10.3 移动操作机器人实施例

本实施例展示影子孪生技术在完整移动操作机器人上的协同应用：

**场景描述**：

- 移动操作机器人需要在仓库中导航到目标位置
- 到达后，机械臂需要抓取指定位置的工件
- 整个过程需要保证人员和设备安全

**协同规划流程**：

1. 底盘影子预演：验证移动路径的安全性
2. 停靠位搜索：在工作空间内搜索满足以下条件的停靠位：
   - 底盘可无碰撞停放
   - 机械臂可达目标工件
   - 机械臂运动不与障碍物碰撞
3. 机械臂影子预演：验证从当前位置到目标抓取姿态的运动安全性
4. 协调执行：底盘和机械臂按规划轨迹协同运动

## 权利要求书

### 独立权利要求

**权利要求1**

一种基于影子孪生的机器人安全防护方法，其特征在于，包括以下步骤：

S1. 创建物理隔离的影子机器人副本，该影子机器人与真实机器人共享相同的运动学参数、物理参数、控制器参数和关节限位配置；

S2. 获取真实机器人当前状态的镜像快照，所述镜像快照包括机器人当前位置、速度、关节角度和末端执行器位置信息；

S3. 将目标命令同时发送到所述影子机器人和命令缓冲队列，所述影子机器人在超前时间窗口内超前执行该目标命令；

S4. 在所述影子机器人执行命令的过程中，实时检测影子机器人与预设障碍物之间的碰撞状态；

S5. 根据所述碰撞检测结果生成安全决策，所述安全决策类型包括允许通行、限速通行和阻断通行；

S6. 若所述安全决策为允许通行或限速通行，则在所述超前时间窗口结束后将所述命令下发至真实机器人；若所述安全决策为阻断通行，则阻止所述命令下发并触发安全警报。

**权利要求2**

一种基于影子孪生的机器人安全防护系统，其特征在于，包括：

影子孪生模块，用于克隆真实机器人并创建物理隔离的影子机器人副本，所述影子机器人与真实机器人共享相同的控制器参数和物理参数；

碰撞检测模块，用于检测所述影子机器人在超前执行命令过程中的碰撞状态；

命令缓冲模块，用于延迟命令执行以实现超前验证，所述命令缓冲模块存储目标命令并根据超前时间窗口控制命令的下发时机；

安全决策模块，用于根据所述碰撞检测结果生成安全决策，并根据所述安全决策控制是否将所述命令下发至真实机器人。

### 从属权利要求

**权利要求3**

根据权利要求1所述的方法，其特征在于，步骤S4中所述碰撞检测包括：

沿命令执行轨迹按预设间距分布的多个采样点；

对每个所述采样点，使用Physics.ComputePenetration精确计算影子机器人与障碍物之间的穿透深度；

根据所述穿透深度判断是否发生碰撞。

**权利要求4**

根据权利要求1所述的方法，其特征在于，步骤S4中所述碰撞检测还包括：

获取影子机器人的多个连杆关键点位置；

沿关节空间轨迹对所述连杆关键点进行采样；

检测所述连杆关键点是否与障碍物发生重叠。

**权利要求5**

根据权利要求1所述的方法，其特征在于，还包括奇异性检测步骤：

计算影子机械臂执行命令过程中的雅可比矩阵；

计算所述雅可比矩阵的行列式；

当所述行列式小于预设阈值时，判定存在奇异性风险并生成阻断决策。

**权利要求6**

根据权利要求5所述的方法，其特征在于，所述奇异性检测还包括多级阈值判断：

当所述行列式小于第一阈值时，生成限速决策；

当所述行列式小于第二阈值时，生成阻断决策。

**权利要求7**

根据权利要求1所述的方法，其特征在于，还包括负载感知的安全边界膨胀步骤：

获取机器人的当前负载状态；

根据所述负载状态动态调整安全边界膨胀系数。

**权利要求8**

根据权利要求1所述的方法，其特征在于，所述超前时间窗口根据以下因素计算：

通信延迟时间；

计算预算时间；

基于制动距离反推的响应时间。

**权利要求9**

根据权利要求2所述的系统，其特征在于，所述影子孪生模块包括：

底盘影子孪生子模块，用于克隆差速驱动底盘并管理底盘命令的超前验证；

机械臂影子孪生子模块，用于克隆多自由度机械臂并管理关节命令的超前验证。

**权利要求10**

根据权利要求2所述的系统，其特征在于，所述安全决策模块进一步包括：

轨迹采样单元，用于沿命令执行轨迹生成采样点序列；

重叠检测单元，用于使用OverlapSphereNonAlloc对所述采样点进行快速碰撞筛选；

精确检测单元，用于对通过重叠检测的采样点使用ComputePenetration进行精确穿透计算。

---

# 摘要

本发明公开了一种基于影子孪生的预测性安全防护方法及系统。所述方法包括：创建物理隔离的影子机器人副本；获取真实机器人当前状态的镜像快照；将目标命令同时发送到影子机器人和命令缓冲队列，影子机器人在超前时间窗口内超前执行该命令；在影子机器人执行命令的过程中，实时检测影子机器人与预设障碍物之间的碰撞状态；根据碰撞检测结果生成允许通行、限速通行或阻断通行三种安全决策；若决策为允许或限速，则在超前时间窗口结束后将命令下发至真实机器人；若决策为阻断，则阻止命令下发并触发安全警报。本发明通过影子孪生超前执行命令，能够在碰撞发生前预测并阻止危险命令，实现主动式安全防护，适用于移动底盘、机械臂及移动操作机器人等多种类型的机器人系统。

---

_文档版本: v1.0_
_生成时间: 2026-05-07_
_基于项目: wheel (Unity 2022.3.52f1c1)_
